// This file was auto-generated by Fern from our API Definition.

export interface CreatePostsPoll {
    /** Duration of the poll in minutes. */
    duration_minutes: number;
    /** Poll options (2-4 choices, 1-25 characters each). */
    options: string[];
    /** Who can reply to the poll Tweet. Accepted for compatibility; it carries no backend argument and is dropped before the mutation is issued (see `translate_body`). */
    reply_settings?: CreatePostsPoll.ReplySettings | undefined;
}

export namespace CreatePostsPoll {
    /** Who can reply to the poll Tweet. Accepted for compatibility; it carries no backend argument and is dropped before the mutation is issued (see `translate_body`). */
    export const ReplySettings = {
        Following: "following",
        MentionedUsers: "mentionedUsers",
        Subscribers: "subscribers",
        Verified: "verified",
    } as const;
    export type ReplySettings = (typeof ReplySettings)[keyof typeof ReplySettings];
}
