---
title: "Google Business Profile models: Performance List Search Keyword Impressions Monthly Response – Verifications Wait For Voice Of Merchant"
description: "38 generated Google Business Profile model contracts, from PerformanceListSearchKeywordImpressionsMonthlyResponse through VerificationsWaitForVoiceOfMerchant."
generated: true
platform: "google-business-profile"
sourceVersion: "v1+legacy-v4"
sidebar:
  order: 1003
  label: "Performance List Search Keyword Impressions Monthly Response – Verifications Wait For Voice Of Merchant"
tableOfContents: false
---

{/* Generated from canonical SdkIr (discovery: packages/google-business-profile-sdk/spec/discovery). Do not edit by hand. */}

## PerformanceListSearchKeywordImpressionsMonthlyResponse

`object` model on the `json` wire format.

Represents the response for ListSearchKeywordImpressionsMonthly.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `nextPageToken` | `nextPageToken` | `string` | No | No | A token indicating the last paginated result returned. This can be used by succeeding requests to get the next "page" of keywords. It will only be present when there are more results to be returned. |
| `searchKeywordsCounts` | `searchKeywordsCounts` | `Array<PerformanceSearchKeywordCount>` | No | No | Search terms which have been used to find a business. |

## PerformanceMultiDailyMetricTimeSeries

`object` model on the `json` wire format.

Represents a list of tuples of DailyMetric-DailySubEntityType-TimeSeries.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `dailyMetricTimeSeries` | `dailyMetricTimeSeries` | `Array<PerformanceDailyMetricTimeSeries>` | No | No | List of DailyMetric-TimeSeries pairs. |

## PerformanceSearchKeywordCount

`object` model on the `json` wire format.

Represents a single search keyword and its value.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `insightsValue` | `insightsValue` | `PerformanceInsightsValue` | No | No | One of either: 1) The sum of the number of unique users that used the keyword in a month, aggregated for each month requested. 2) A threshold that indicates that the actual value is below this threshold. |
| `searchKeyword` | `searchKeyword` | `string` | No | No | The lower-cased string that the user entered. |

## PerformanceTimeOfDay

`object` model on the `json` wire format.

Represents a time of day. The date and time zone are either not significant or are specified elsewhere. An API may choose to allow leap seconds. Related types are google.type.Date and `google.protobuf.Timestamp`.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `hours` | `hours` | `integer` | No | No | Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value "24:00:00" for scenarios like business closing time. |
| `minutes` | `minutes` | `integer` | No | No | Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59. |
| `nanos` | `nanos` | `integer` | No | No | Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999. |
| `seconds` | `seconds` | `integer` | No | No | Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds. |

## PerformanceTimeSeries

`object` model on the `json` wire format.

Represents a timeseries.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `datedValues` | `datedValues` | `Array<PerformanceDatedValue>` | No | No | List of datapoints in the timeseries, where each datapoint is a date-value pair. |

## PlaceActionsEmpty

`object` model on the `json` wire format.

A generic empty message that you can re-use to avoid defining duplicated empty messages in your APIs. A typical example is to use it as the request or the response type of an API method. For instance: service Foo &#123; rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty); &#125;

No fields are declared.

## PlaceActionsListPlaceActionLinksResponse

`object` model on the `json` wire format.

Response message for PlaceActions.ListPlaceActionLinks.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `nextPageToken` | `nextPageToken` | `string` | No | No | If there are more place action links than the requested page size, then this field is populated with a token to fetch the next page of results. |
| `placeActionLinks` | `placeActionLinks` | `Array<PlaceActionsPlaceActionLink>` | No | No | The returned list of place action links. |

## PlaceActionsListPlaceActionTypeMetadataResponse

`object` model on the `json` wire format.

Response message for PlaceActions.ListPlaceActionTypeMetadata.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `nextPageToken` | `nextPageToken` | `string` | No | No | If the number of action types exceeded the requested page size, this field will be populated with a token to fetch the next page on a subsequent call to `placeActionTypeMetadata.list`. If there are no more results, this field will not be present in the response. |
| `placeActionTypeMetadata` | `placeActionTypeMetadata` | `Array<PlaceActionsPlaceActionTypeMetadata>` | No | No | A collection of metadata for the available place action types. |

## PlaceActionsPlaceActionLink

`object` model on the `json` wire format.

Represents a place action link and its attributes.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `createTime` | `createTime` | `string` | No | No | Output only. The time when the place action link was created. |
| `isEditable` | `isEditable` | `boolean` | No | No | Output only. Indicates whether this link can be edited by the client. |
| `isPreferred` | `isPreferred` | `boolean` | No | No | Optional. Whether this link is preferred by the merchant. Only one link can be marked as preferred per place action type at a location. If a future request marks a different link as preferred for the same place action type, then the current preferred link (if any exists) will lose its preference. |
| `name` | `name` | `string` | No | No | Optional. The resource name, in the format `locations/&#123;location_id&#125;/placeActionLinks/&#123;place_action_link_id&#125;`. The name field will only be considered in UpdatePlaceActionLink and DeletePlaceActionLink requests for updating and deleting links respectively. However, it will be ignored in CreatePlaceActionLink request, where `place_action_link_id` will be assigned by the server on successful creation of a new link and returned as part of the response. |
| `placeActionType` | `placeActionType` | `"PLACE_ACTION_TYPE_UNSPECIFIED" \| "APPOINTMENT" \| "ONLINE_APPOINTMENT" \| "DINING_RESERVATION" \| "FOOD_ORDERING" \| "FOOD_DELIVERY" \| "FOOD_TAKEOUT" \| "SHOP_ONLINE" \| "SOLOPRENEUR_APPOINTMENT"` | No | No | Required. The type of place action that can be performed using this link. |
| `providerType` | `providerType` | `"PROVIDER_TYPE_UNSPECIFIED" \| "MERCHANT" \| "AGGREGATOR_3P"` | No | No | Output only. Specifies the provider type. |
| `updateTime` | `updateTime` | `string` | No | No | Output only. The time when the place action link was last modified. |
| `uri` | `uri` | `string` | No | No | Required. The link uri. The same uri can be reused for different action types across different locations. However, only one place action link is allowed for each unique combination of (uri, place action type, location). |

## PlaceActionsPlaceActionTypeMetadata

`object` model on the `json` wire format.

Metadata for supported place action types.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `displayName` | `displayName` | `string` | No | No | The localized display name for the attribute, if available; otherwise, the English display name. |
| `placeActionType` | `placeActionType` | `"PLACE_ACTION_TYPE_UNSPECIFIED" \| "APPOINTMENT" \| "ONLINE_APPOINTMENT" \| "DINING_RESERVATION" \| "FOOD_ORDERING" \| "FOOD_DELIVERY" \| "FOOD_TAKEOUT" \| "SHOP_ONLINE" \| "SOLOPRENEUR_APPOINTMENT"` | No | No | The place action type. |

## QAndAAnswer

`object` model on the `json` wire format.

Represents an answer to a question

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `author` | `author` | `QAndAAuthor` | No | No | Output only. The author of the answer. Will only be set during list operations. |
| `createTime` | `createTime` | `string` | No | No | Output only. The timestamp for when the answer was written. Only retrieved during ListResponse fetching. |
| `name` | `name` | `string` | No | No | Output only. The unique name for the answer locations/*/questions/*/answers/* |
| `text` | `text` | `string` | No | No | Required. The text of the answer. It should contain at least one non-whitespace character. The maximum length is 4096 characters. |
| `updateTime` | `updateTime` | `string` | No | No | Output only. The timestamp for when the answer was last modified. |
| `upvoteCount` | `upvoteCount` | `integer` | No | No | Output only. The number of upvotes for the answer. |

## QAndAAuthor

`object` model on the `json` wire format.

Represents the author of a question or answer

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `displayName` | `displayName` | `string` | No | No | The display name of the user |
| `profilePhotoUri` | `profilePhotoUri` | `string` | No | No | The profile photo URI of the user. |
| `type` | `type` | `"AUTHOR_TYPE_UNSPECIFIED" \| "REGULAR_USER" \| "LOCAL_GUIDE" \| "MERCHANT"` | No | No | The type of user the author is. |

## QAndAEmpty

`object` model on the `json` wire format.

A generic empty message that you can re-use to avoid defining duplicated empty messages in your APIs. A typical example is to use it as the request or the response type of an API method. For instance: service Foo &#123; rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty); &#125;

No fields are declared.

## QAndAListAnswersResponse

`object` model on the `json` wire format.

Response message for QuestionsAndAnswers.ListAnswers

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `answers` | `answers` | `Array<QAndAAnswer>` | No | No | The requested answers. |
| `nextPageToken` | `nextPageToken` | `string` | No | No | If the number of answers exceeds the requested max page size, this field is populated with a token to fetch the next page of answers on a subsequent call. If there are no more answers, this field is not present in the response. |
| `totalSize` | `totalSize` | `integer` | No | No | The total number of answers posted for this question across all pages. |

## QAndAListQuestionsResponse

`object` model on the `json` wire format.

Response message for QuestionsAndAnswers.ListQuestions

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `nextPageToken` | `nextPageToken` | `string` | No | No | If the number of questions exceeds the requested max page size, this field is populated with a token to fetch the next page of questions on a subsequent call. If there are no more questions, this field is not present in the response. |
| `questions` | `questions` | `Array<QAndAQuestion>` | No | No | The requested questions, |
| `totalSize` | `totalSize` | `integer` | No | No | The total number of questions posted for this location across all pages. |

## QAndAQuestion

`object` model on the `json` wire format.

Represents a single question and some of its answers.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `author` | `author` | `QAndAAuthor` | No | No | Output only. The author of the question. |
| `createTime` | `createTime` | `string` | No | No | Output only. The timestamp for when the question was written. |
| `name` | `name` | `string` | No | No | Immutable. The unique name for the question. locations/*/questions/* This field will be ignored if set during question creation. |
| `text` | `text` | `string` | No | No | Required. The text of the question. It should contain at least three words and the total length should be greater than or equal to 10 characters. The maximum length is 4096 characters. |
| `topAnswers` | `topAnswers` | `Array<QAndAAnswer>` | No | No | Output only. A list of answers to the question, sorted by upvotes. This may not be a complete list of answers depending on the request parameters (answers_per_question) |
| `totalAnswerCount` | `totalAnswerCount` | `integer` | No | No | Output only. The total number of answers posted for this question. |
| `updateTime` | `updateTime` | `string` | No | No | Output only. The timestamp for when the question was last modified. |
| `upvoteCount` | `upvoteCount` | `integer` | No | No | Output only. The number of upvotes for the question. |

## QAndAUpsertAnswerRequest

`object` model on the `json` wire format.

Request message for QuestionsAndAnswers.UpsertAnswer

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `answer` | `answer` | `QAndAAnswer` | No | No | Required. The new answer. |

## VerificationsAddressVerificationData

`object` model on the `json` wire format.

Display data for verifications through postcard.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `address` | `address` | `VerificationsPostalAddress` | No | No | Address that a postcard can be sent to. |
| `business` | `business` | `string` | No | No | Merchant's business name. |
| `expectedDeliveryDaysRegion` | `expectedDeliveryDaysRegion` | `integer` | No | No | Expected number of days it takes to deliver a postcard to the address's region. |

## VerificationsCompleteVerificationRequest

`object` model on the `json` wire format.

Request message for Verifications.CompleteVerificationAction.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `pin` | `pin` | `string` | No | No | Required. PIN code received by the merchant to complete the verification. |

## VerificationsCompleteVerificationResponse

`object` model on the `json` wire format.

Response message for Verifications.CompleteVerificationAction.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `verification` | `verification` | `VerificationsVerification` | No | No | The completed verification. |

## VerificationsComplyWithGuidelines

`object` model on the `json` wire format.

Indicates that the location fails to comply with our [guidelines](https://support.google.com/business/answer/3038177).

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `recommendationReason` | `recommendationReason` | `"RECOMMENDATION_REASON_UNSPECIFIED" \| "BUSINESS_LOCATION_SUSPENDED" \| "BUSINESS_LOCATION_DISABLED"` | No | No | The reason why the location is being recommended to comply with guidelines. |

## VerificationsEmailVerificationData

`object` model on the `json` wire format.

Display data for verifications through email.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `domain` | `domain` | `string` | No | No | Domain name in the email address. e.g. "gmail.com" in foo@gmail.com |
| `isUserNameEditable` | `isUserNameEditable` | `boolean` | No | No | Whether client is allowed to provide a different user name. |
| `user` | `user` | `string` | No | No | User name in the email address. e.g. "foo" in foo@gmail.com |

## VerificationsFetchVerificationOptionsRequest

`object` model on the `json` wire format.

Request message for Verifications.FetchVerificationOptions.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `context` | `context` | `VerificationsServiceBusinessContext` | No | No | Optional. Extra context information for the verification of service businesses. Can only be applied to the locations whose business type is CUSTOMER_LOCATION_ONLY. Specifying an accurate address could enable more options. INVALID_ARGUMENT will be thrown if it is set for other business types of locations. |
| `languageCode` | `languageCode` | `string` | No | No | Required. The BCP 47 language code representing the language that is to be used for the verification process. Available options vary by language. |

## VerificationsFetchVerificationOptionsResponse

`object` model on the `json` wire format.

Response message for Verifications.FetchVerificationOptions.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `options` | `options` | `Array<VerificationsVerificationOption>` | No | No | The available verification options. |

## VerificationsGenerateInstantVerificationTokenRequest

`object` model on the `json` wire format.

Request message for Verifications.GenerateInstantVerificationToken.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `locationId` | `locationId` | `string` | No | No | The location identifier associated with an unverified listing. This is the location id generated at the time that the listing was originally created. It is the final portion of a location resource name as generated by the Google My Business API. Note: the caller must be an owner or manager of this listing in order to generate a verification token. See the location resource documentation for more information. |

## VerificationsGenerateInstantVerificationTokenResponse

`object` model on the `json` wire format.

Response message for Verifications.GenerateInstantVerificationToken.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `instantVerificationToken` | `instantVerificationToken` | `string` | No | No | The generated instant verification token. |
| `result` | `result` | `"RESULT_UNSPECIFIED" \| "SUCCEEDED" \| "FAILED"` | No | No | Output only. The result of the instant verification token generation. |

## VerificationsListVerificationsResponse

`object` model on the `json` wire format.

Response message for Verifications.ListVerifications.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `nextPageToken` | `nextPageToken` | `string` | No | No | If the number of verifications exceeded the requested page size, this field will be populated with a token to fetch the next page of verification on a subsequent call. If there are no more attributes, this field will not be present in the response. |
| `verifications` | `verifications` | `Array<VerificationsVerification>` | No | No | List of the verifications. |

## VerificationsPostalAddress

`object` model on the `json` wire format.

Represents a postal address, such as for postal delivery or payments addresses. With a postal address, a postal service can deliver items to a premise, P.O. box, or similar. A postal address is not intended to model geographical locations like roads, towns, or mountains. In typical usage, an address would be created by user input or from importing existing data, depending on the type of process. Advice on address input or editing: - Use an internationalization-ready address widget such as https://github.com/google/libaddressinput. - Users should not be presented with UI elements for input or editing of fields outside countries where that field is used. For more guidance on how to use this schema, see: https://support.google.com/business/answer/6397478.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `addressLines` | `addressLines` | `Array<string>` | No | No | Unstructured address lines describing the lower levels of an address. Because values in `address_lines` do not have type information and may sometimes contain multiple values in a single field (for example, "Austin, TX"), it is important that the line order is clear. The order of address lines should be "envelope order" for the country or region of the address. In places where this can vary (for example, Japan), `address_language` is used to make it explicit (for example, "ja" for large-to-small ordering and "ja-Latn" or "en" for small-to-large). In this way, the most specific line of an address can be selected based on the language. The minimum permitted structural representation of an address consists of a `region_code` with all remaining information placed in the `address_lines`. It would be possible to format such an address very approximately without geocoding, but no semantic reasoning could be made about any of the address components until it was at least partially resolved. Creating an address only containing a `region_code` and `address_lines` and then geocoding is the recommended way to handle completely unstructured addresses (as opposed to guessing which parts of the address should be localities or administrative areas). |
| `administrativeArea` | `administrativeArea` | `string` | No | No | Optional. Highest administrative subdivision which is used for postal addresses of a country or region. For example, this can be a state, a province, an oblast, or a prefecture. For Spain, this is the province and not the autonomous community (for example, "Barcelona" and not "Catalonia"). Many countries don't use an administrative area in postal addresses. For example, in Switzerland, this should be left unpopulated. |
| `languageCode` | `languageCode` | `string` | No | No | Optional. BCP-47 language code of the contents of this address (if known). This is often the UI language of the input form or is expected to match one of the languages used in the address' country/region, or their transliterated equivalents. This can affect formatting in certain countries, but is not critical to the correctness of the data and will never affect any validation or other non-formatting related operations. If this value is not known, it should be omitted (rather than specifying a possibly incorrect default). Examples: "zh-Hant", "ja", "ja-Latn", "en". |
| `locality` | `locality` | `string` | No | No | Optional. Generally refers to the city or town portion of the address. Examples: US city, IT comune, UK post town. In regions of the world where localities are not well defined or do not fit into this structure well, leave `locality` empty and use `address_lines`. |
| `organization` | `organization` | `string` | No | No | Optional. The name of the organization at the address. |
| `postalCode` | `postalCode` | `string` | No | No | Optional. Postal code of the address. Not all countries use or require postal codes to be present, but where they are used, they may trigger additional validation with other parts of the address (for example, state or zip code validation in the United States). |
| `recipients` | `recipients` | `Array<string>` | No | No | Optional. The recipient at the address. This field may, under certain circumstances, contain multiline information. For example, it might contain "care of" information. |
| `regionCode` | `regionCode` | `string` | No | No | Required. CLDR region code of the country/region of the address. This is never inferred and it is up to the user to ensure the value is correct. See https://cldr.unicode.org/ and https://www.unicode.org/cldr/charts/30/supplemental/territory_information.html for details. Example: "CH" for Switzerland. |
| `revision` | `revision` | `integer` | No | No | The schema revision of the `PostalAddress`. This must be set to 0, which is the latest revision. All new revisions **must** be backward compatible with old revisions. |
| `sortingCode` | `sortingCode` | `string` | No | No | Optional. Additional, country-specific, sorting code. This is not used in most regions. Where it is used, the value is either a string like "CEDEX", optionally followed by a number (for example, "CEDEX 7"), or just a number alone, representing the "sector code" (Jamaica), "delivery area indicator" (Malawi) or "post office indicator" (Côte d'Ivoire). |
| `sublocality` | `sublocality` | `string` | No | No | Optional. Sublocality of the address. For example, this can be a neighborhood, borough, or district. |

## VerificationsResolveOwnershipConflict

`object` model on the `json` wire format.

Indicates that the location duplicates another location that is in good standing.

No fields are declared.

## VerificationsServiceBusinessContext

`object` model on the `json` wire format.

Additional data for service business verification.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `address` | `address` | `VerificationsPostalAddress` | No | No | The verification address of the location. It is used to either enable more verification options or send a postcard. |

## VerificationsVerification

`object` model on the `json` wire format.

A verification represents a verification attempt on a location.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `announcement` | `announcement` | `string` | No | No | Optional. Response announcement set only if the method is VETTED_PARTNER. |
| `createTime` | `createTime` | `string` | No | No | The timestamp when the verification is requested. |
| `method` | `method` | `"VERIFICATION_METHOD_UNSPECIFIED" \| "ADDRESS" \| "EMAIL" \| "PHONE_CALL" \| "SMS" \| "AUTO" \| "TRUSTED_PARTNER"` | No | No | The method of the verification. |
| `name` | `name` | `string` | No | No | Resource name of the verification. |
| `state` | `state` | `"STATE_UNSPECIFIED" \| "PENDING" \| "COMPLETED" \| "FAILED"` | No | No | The state of the verification. |

## VerificationsVerificationOption

`object` model on the `json` wire format.

The verification option represents how to verify the location (indicated by verification method) and where the verification will be sent to (indicated by display data).

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `addressData` | `addressData` | `VerificationsAddressVerificationData` | No | No | Set only if the method is MAIL. |
| `announcement` | `announcement` | `string` | No | No | Set only if the method is VETTED_PARTNER. |
| `emailData` | `emailData` | `VerificationsEmailVerificationData` | No | No | Set only if the method is EMAIL. |
| `phoneNumber` | `phoneNumber` | `string` | No | No | Set only if the method is PHONE_CALL or SMS. Phone number that the PIN will be sent to. |
| `verificationMethod` | `verificationMethod` | `"VERIFICATION_METHOD_UNSPECIFIED" \| "ADDRESS" \| "EMAIL" \| "PHONE_CALL" \| "SMS" \| "AUTO" \| "TRUSTED_PARTNER"` | No | No | Method to verify the location. |

## VerificationsVerificationToken

`object` model on the `json` wire format.

Token generated by a vetted [partner](https://support.google.com/business/answer/7674102).

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `tokenString` | `tokenString` | `string` | No | No | The token string. |

## VerificationsVerify

`object` model on the `json` wire format.

Indicates that the location requires verification. Contains information about the current verification actions performed on the location.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `hasPendingVerification` | `hasPendingVerification` | `boolean` | No | No | Indicates whether a verification process has already started, and can be completed by the location. |

## VerificationsVerifyLocationRequest

`object` model on the `json` wire format.

Request message for Verifications.VerifyLocation.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `context` | `context` | `VerificationsServiceBusinessContext` | No | No | Optional. Extra context information for the verification of service businesses. It is only required for the locations whose business type is CUSTOMER_LOCATION_ONLY. For ADDRESS verification, the address will be used to send out postcard. For other methods, it should be the same as the one that is passed to GetVerificationOptions. INVALID_ARGUMENT will be thrown if it is set for other types of business locations. |
| `emailAddress` | `emailAddress` | `string` | No | No | Optional. The input for EMAIL method. Email address where the PIN should be sent to. An email address is accepted only if it is one of the addresses provided by FetchVerificationOptions. If the EmailVerificationData has is_user_name_editable set to true, the client may specify a different user name (local-part) but must match the domain name. |
| `languageCode` | `languageCode` | `string` | No | No | Optional. The BCP 47 language code representing the language that is to be used for the verification process. |
| `mailerContact` | `mailerContact` | `string` | No | No | Optional. The input for ADDRESS method. Contact name the mail should be sent to. |
| `method` | `method` | `"VERIFICATION_METHOD_UNSPECIFIED" \| "ADDRESS" \| "EMAIL" \| "PHONE_CALL" \| "SMS" \| "AUTO" \| "TRUSTED_PARTNER"` | No | No | Required. Verification method. |
| `phoneNumber` | `phoneNumber` | `string` | No | No | Optional. The input for PHONE_CALL/SMS method The phone number that should be called or be sent SMS to. It must be one of the phone numbers in the eligible options. |
| `token` | `token` | `VerificationsVerificationToken` | No | No | Optional. The input for VETTED_PARTNER method available to select [partners.](https://support.google.com/business/answer/7674102) The input is not needed for a vetted account. Token that is associated to the location. Token that is associated to the location. |
| `trustedPartnerToken` | `trustedPartnerToken` | `string` | No | No | The input for TRUSTED_PARTNER method The verification token that is associated to the location. |

## VerificationsVerifyLocationResponse

`object` model on the `json` wire format.

Response message for Verifications.VerifyLocation.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `verification` | `verification` | `VerificationsVerification` | No | No | The created verification request. |

## VerificationsVoiceOfMerchantState

`object` model on the `json` wire format.

Response message for VoiceOfMerchant.GetVoiceOfMerchantState.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `complyWithGuidelines` | `complyWithGuidelines` | `VerificationsComplyWithGuidelines` | No | No | The location fails to comply with our [guidelines](https://support.google.com/business/answer/3038177) and requires additional steps for reinstatement. To fix this issue, consult the [Help Center Article](https://support.google.com/business/answer/4569145). |
| `hasBusinessAuthority` | `hasBusinessAuthority` | `boolean` | No | No | Indicates whether the location has the authority (ownership) over the business on Google. If true, another location cannot take over and become the dominant listing on Maps. However, edits will not become live unless Voice of Merchant is gained (i.e. has_voice_of_merchant is true). |
| `hasVoiceOfMerchant` | `hasVoiceOfMerchant` | `boolean` | No | No | Indicates whether the location is in good standing and has control over the business on Google. Any edits made to the location will propagate to Maps after passing the review phase. |
| `resolveOwnershipConflict` | `resolveOwnershipConflict` | `VerificationsResolveOwnershipConflict` | No | No | This location duplicates another location that is in good standing. If you have access to the location in good standing, use that location's id to perform operations. Otherwise, request access from the current owner. |
| `verify` | `verify` | `VerificationsVerify` | No | No | Start or continue the verification process. |
| `waitForVoiceOfMerchant` | `waitForVoiceOfMerchant` | `VerificationsWaitForVoiceOfMerchant` | No | No | Wait to gain Voice of Merchant. The location is under review for quality purposes. |

## VerificationsWaitForVoiceOfMerchant

`object` model on the `json` wire format.

Indicates that the location will gain voice of merchant after passing review.

No fields are declared.
