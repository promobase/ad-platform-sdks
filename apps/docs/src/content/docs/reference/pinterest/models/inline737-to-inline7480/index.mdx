---
title: "Pinterest models: Inline737 – Inline7480"
description: "100 generated Pinterest model contracts, from Inline737 through Inline7480."
generated: true
platform: "pinterest"
sourceVersion: "v5"
sidebar:
  order: 1084
  label: "Inline737 – Inline7480"
tableOfContents: false
---

{/* Generated from canonical SdkIr (openapi: https://github.com/pinterest/api-description). Do not edit by hand. */}

## Inline737

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `ITEM_ID` | `ITEM_ID` | `Inline737` | Yes | No | — |

## Inline7370

`object` model on the `json` wire format.

No fields are declared.

## Inline7371

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AGE_BUCKET` | `AGE_BUCKET` | `Array<string>` | No | No | **Legacy field.** Predefined age ranges. We recommend using MINIMUM_AGE and MAXIMUM_AGE instead for more flexible targeting. Cannot be combined with MINIMUM_AGE/MAXIMUM_AGE. If neither AGE_BUCKET nor MINIMUM_AGE/MAXIMUM_AGE are specified, all ages will be targeted. |
| `APPTYPE` | `APPTYPE` | `Array<string>` | No | No | Allowed devices. If the APPTYPE field is missing, the default behavior in terms of ad delivery is that **All devices/apptypes** will be targeted. |
| `AUDIENCE_EXCLUDE` | `AUDIENCE_EXCLUDE` | `Array<unknown>` | No | No | Excluded customer list IDs. Used to drive new customer acquisition goals. For example: ["2542620905475"]. Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_EXCLUDE field is missing, the default behavior in terms of ad delivery is that **No users will be excluded**. |
| `AUDIENCE_INCLUDE` | `AUDIENCE_INCLUDE` | `Array<unknown>` | No | No | Targeted customer list IDs. For example: ["2542620905473"]. Audience lists need to have at least 100 people with Pinterest accounts in them Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_INCLUDE field is missing, the default behavior in terms of ad delivery is that **All users will be included**. |
| `GENDER` | `GENDER` | `Array<string>` | No | No | Targeted genders. Values: ["unknown","male","female"]. If the GENDER field is missing, the default behavior in terms of ad delivery is that **All genders will be targeted**. |
| `GEO` | `GEO` | `Array<string>` | No | No | Region codes or postal codes to include for targeting. Region codes represent broader geographical areas. Example: `US-CA` is the region code for California in the United States. Postal codes represent more granular, specific areas. Example: `94103` is a postal code for a specifc area in San Francisco, California, U.S.A. For each ad group, use only one of these methods, depending on which fits your targeting needs. Do not use both. For example, either specify a broader region code like `US-CA` or a more granular postal code within that regon, such as `94103`. You can specify multiple region codes or postal codes in an array, depending on which method you choose. Precede a region code array with the `region_codes` key and a postal code value with the `postal_codes` key. Examples: ``` "geo": &#123; "region_codes": ["US-CA"] &#125; ``` ``` "geo": &#123; "postal_codes": ["94103"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION`. If you do not specifiy a `GEO` code, only `LOCATION` values will be targeted (See `LOCATION` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `GEO_EXCLUDE` | `GEO_EXCLUDE` | `Array<string>` | No | No | Closed beta Region codes or postal codes to exclude from the targeting inclusion area. See `GEO` parameter in this targeting spec for rules, syntax, and other information. |
| `INTEREST` | `INTEREST` | `Array<string>` | No | No | Array of interest object IDs. If the INTEREST field is missing, the default behavior in terms of ad delivery is that **All interests will be targeted**. |
| `LOCALE` | `LOCALE` | `Array<string>` | No | No | 24 ISO 639-1 two-letter language codes. If the LOCALE field is not included in the request, all languages are targeted. |
| `LOCATION` | `LOCATION` | `Array<string>` | No | No | Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to include for targeting. Precede country code values with the `country_codes` key and metro code values with `metro_codes` key. Example: ``` "location": &#123; "country_codes": ["US", "CA"], "metro_codes": ["501", "602"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION` code. If you do not specify a `LOCATION` code, only `GEO` values will be targeted (See `GEO` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `LOCATION_EXCLUDE` | `LOCATION_EXCLUDE` | `Array<string>` | No | No | Closed beta Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to exclude from targeting. See `LOCATION` parameter in this targeting spec for rules, syntax, and other information. |
| `MAXIMUM_AGE` | `MAXIMUM_AGE` | `string` | No | No | Maximum age to target (inclusive). Values: "18", "19", ..., "65", "65+". Must be used together with `MINIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `MINIMUM_AGE` | `MINIMUM_AGE` | `string` | No | No | Minimum age to target (inclusive). Values: "18", "19", ..., "65". Note: 65+ is not allowed for minimum age. Must be used together with `MAXIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `SHOPPING_RETARGETING` | `SHOPPING_RETARGETING` | `Array<Inline7371>` | No | No | Array of object: lookback_window [Integer]: Number of days ago to start lookback timeframe for dynamic retargeting tag_types [Array of integer]: Event types to target for dynamic retargeting exclusion_window [Integer]: Number of days ago to stop lookback timeframe for dynamic retargeting |
| `TARGETING_STRATEGY` | `TARGETING_STRATEGY` | `Array<string>` | No | No | — |

## Inline7372

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline7374

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `auto_targeting_enabled` | `auto_targeting_enabled` | `boolean` | No | No | Enable auto-targeting for ad group. Default value is True. Also known as ["Performance+ targeting"](https://help.pinterest.com/en/business/article/performance-plus-targeting). |
| `bid_in_micro_currency` | `bid_in_micro_currency` | `number` | No | No | Bid price in micro currency. This field is **REQUIRED** for the following campaign objective_type/billable_event combinations: AWARENESS/IMPRESSION, CONSIDERATION/CLICKTHROUGH, CATALOG_SALES/CLICKTHROUGH, VIDEO_VIEW/VIDEO_V_50_MRC. |
| `bid_multiplier` | `bid_multiplier` | `number` | No | No | [Open beta](https://help.pinterest.com/en/business/article/beta-features) Bid multiplier for ad group. This value is a double between 0.1 and 10.0. Enter 0 to remove the bid multiplier. - Not currently supported for [Performance+ campaigns](https://help.pinterest.com/en/business/article/performance-plus-campaigns). |
| `bid_strategy_type` | `bid_strategy_type` | `string` | No | No | Bid strategy type. For Campaigns with Video Completion objectives, the only supported bid strategy type is AUTOMATIC_BID, also known as "Pinterest Performance+ bidding". |
| `billable_event` | `billable_event` | `string` | No | No | Ad group billable event type. For update, only draft ad groups may update billable event. |
| `budget_in_micro_currency` | `budget_in_micro_currency` | `number` | No | No | Budget in micro currency. This field is **REQUIRED** for non-CBO (campaign budget optimization) campaigns. A CBO campaign automatically generates ad group budgets from its campaign budget to maximize campaign outcome. A CBO campaign is limited to 70 or less ad groups. |
| `budget_type` | `budget_type` | `string` | No | No | Budget type. If DAILY, an ad group's daily spend will not exceed the budget parameter value. If LIFETIME, the end_time parameter is **REQUIRED**, and the ad group spend is spread evenly between the ad group `start_time` and `end_time` range. A CBO campaign automatically generates ad group budgets from its campaign budget to maximize campaign outcome. For CBO campaigns, only "CBO_ADGROUP" is allowed. For WEB_SESSIONS campaigns, only "LIFETIME" is allowed. For update, only draft ad groups may update budget type. |
| `campaign_id` | `campaign_id` | `string` | No | No | Campaign ID of the ad group. |
| `customer_segment_id` | `customer_segment_id` | `Inline7374` | No | No | Customer segment ID applied to the ad group. We currently only support 1 customer segment per ad group. To use customer segments, do not set the `targeting_template_ids` field as well as the `AUDIENCE_INCLUDE` field of the `targeting_spec`. To clear the customer segment ID, set this field to '0'. |
| `end_time` | `end_time` | `number` | No | No | Timestamp in Unix format for scheduling when ads in the ad group stop appearing. If not specified, ads run indefinitely unless you update the ad group by changing their status to `paused`. Cannot occur after `end_time` for parent campaign (if specified). Learn about [scheduling ads](https://help.pinterest.com/en/business/article/ads-manager-schedule-ads). For certain organizations ([Closed beta](https://help.pinterest.com/en/business/article/beta-features)): Supported for campaigns with Campaign Budget Optimization (CBO). For all organizations: Supported for campaigns without CBO. |
| `ext_features` | `ext_features` | `Inline7375` | No | No | — |
| `feed_profile_id` | `feed_profile_id` | `string` | No | No | Feed Profile ID associated to the adgroup. |
| `id` | `id` | `string` | Yes | No | Ad group ID. |
| `is_creative_optimization` | `is_creative_optimization` | `boolean` | No | No | Enable creative optimization for the ad group, default value is FALSE. When enabled, you allow Pinterest to automatically turn your product Pins into ads in different formats (collections and shopping) and deliver those ads to users at scale. |
| `is_local_inventory` | `is_local_inventory` | `boolean` | No | No | Indicates whether the ad group should use the local inventory. |
| `lifetime_frequency_cap` | `lifetime_frequency_cap` | `number` | No | No | Set a limit to the number of times a promoted pin from this campaign can be impressed by a pinner within the past rolling 30 days. Only available for CPM (cost per mille (1000 impressions)) ad groups. A CPM ad group has an IMPRESSION [billable_event](https://help.pinterest.com/en/business/article/billable-events) value. This field **REQUIRES** the `end_time` field. |
| `local_inventory_radius_in_miles` | `local_inventory_radius_in_miles` | `number` | No | No | The targeting radius of the local inventory ads in miles. |
| `name` | `name` | `string` | No | No | Ad group name. |
| `optimization_goal_metadata` | `optimization_goal_metadata` | `Inline7376` | No | No | Optimization goals for objective-based performance campaigns. **REQUIRED** when campaign's `objective_type` is set to `"WEB_CONVERSION"`. |
| `pacing_delivery_type` | `pacing_delivery_type` | `string` | No | No | Ad group pacing delivery type. With ACCELERATED, an ad group budget is spent as fast as possible. With STANDARD, an ad group budget is spent smoothly over a day. When using CBO, only the STANDARD pacing delivery type is allowed. |
| `performance_plus_campaign_settings` | `performance_plus_campaign_settings` | `Inline7379` | No | No | Pinterest Performance+ campaign settings. |
| `placement_group` | `placement_group` | `Inline7380` | No | No | [Placement group](https://help.pinterest.com/en/business/article/placement-groups). |
| `placement_traffic_type` | `placement_traffic_type` | `Inline7381` | No | No | A targeting option that enables advertisers to choose whether to run ads in fullscreen feed, two column feed, or both |
| `promotion_application_level` | `promotion_application_level` | `Inline7382` | No | No | Specify if the promotion is applied at ad group or item level |
| `promotion_id` | `promotion_id` | `string` | No | No | Promotion ID. To clear this field, set to null. |
| `promotion_ids` | `promotion_ids` | `Array<string>` | No | No | Promotion IDs list. To clear this field, set to an empty array []. |
| `start_time` | `start_time` | `number` | No | No | Timestamp in Unix format for scheduling when ads in the ad group start to appear. If not specified, ads appear during parent campaign's `start_time`. Cannot precede `start_time` for parent campaign (if specified). Learn about scheduling ads. For certain organizations (Closed beta): Supported for campaigns with Campaign Budget Optimization (CBO). For all organizations: Supported for campaigns without CBO. |
| `status` | `status` | `Inline7383` | No | No | Ad group/entity status. |
| `targeting_spec` | `targeting_spec` | `Inline7384` | No | No | — |
| `targeting_spec_operations` | `targeting_spec_operations` | `Array<Inline7385 \| Inline7386 \| Inline7387 \| Inline7388 \| Inline7389 \| Inline7390 \| Inline7391 \| Inline7392 \| Inline7393 \| Inline7394 \| Inline7395 \| Inline7396 \| Inline7397 \| Inline7398>` | No | No | — |
| `targeting_template_ids` | `targeting_template_ids` | `Array<unknown>` | No | No | Targeting template IDs applied to the ad group. We currently only support 1 targeting template per ad group. To use targeting templates, do not set any other targeting fields: targeting_spec, tracking_urls, auto_targeting_enabled, placement_group. To clear all targeting template IDs, set this field to ['0']. |
| `tracking_urls` | `tracking_urls` | `Inline7399` | No | No | Third-party tracking URLs. Up to three tracking URLs - with a max length of 2,000 - are supported for each event type. Tracking URLs set at the ad group or ad level can override those set at the campaign level. For more information, see [Third-party and dynamic tracking](https://help.pinterest.com/en/business/article/third-party-and-dynamic-tracking). |

## Inline7375

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `enabled` | `enabled` | `Array<string>` | No | No | Tracking features. To clear this field, set to null. |

## Inline7376

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `conversion_tag_v3_goal_metadata` | `conversion_tag_v3_goal_metadata` | `Inline7376` | No | No | — |
| `frequency_goal_metadata` | `frequency_goal_metadata` | `Inline7377` | No | No | Frequency target can only be between 2 and 20 |
| `scrollup_goal_metadata` | `scrollup_goal_metadata` | `Inline7378` | No | No | — |

## Inline7377

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `frequency` | `frequency` | `number` | No | No | — |
| `timerange` | `timerange` | `string` | No | No | — |

## Inline7378

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `scrollup_goal_value_in_micro_currency` | `scrollup_goal_value_in_micro_currency` | `string` | No | No | — |

## Inline7379

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `boost_prospecting_ad_group_bid` | `boost_prospecting_ad_group_bid` | `boolean` | No | No | Whether to boost prospecting ad group bid. |
| `pinner_list_exclusions` | `pinner_list_exclusions` | `Array<string>` | No | No | List of campaign-level exclusion pinner list IDs. |

## Inline738

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AVAILABILITY` | `AVAILABILITY` | `Inline738` | Yes | No | — |

## Inline7380

`object` model on the `json` wire format.

No fields are declared.

## Inline7381

`object` model on the `json` wire format.

No fields are declared.

## Inline7382

`object` model on the `json` wire format.

No fields are declared.

## Inline7383

`object` model on the `json` wire format.

No fields are declared.

## Inline7384

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AGE_BUCKET` | `AGE_BUCKET` | `Array<string>` | No | No | **Legacy field.** Predefined age ranges. We recommend using MINIMUM_AGE and MAXIMUM_AGE instead for more flexible targeting. Cannot be combined with MINIMUM_AGE/MAXIMUM_AGE. If neither AGE_BUCKET nor MINIMUM_AGE/MAXIMUM_AGE are specified, all ages will be targeted. |
| `APPTYPE` | `APPTYPE` | `Array<string>` | No | No | Allowed devices. If the APPTYPE field is missing, the default behavior in terms of ad delivery is that **All devices/apptypes** will be targeted. |
| `AUDIENCE_EXCLUDE` | `AUDIENCE_EXCLUDE` | `Array<unknown>` | No | No | Excluded customer list IDs. Used to drive new customer acquisition goals. For example: ["2542620905475"]. Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_EXCLUDE field is missing, the default behavior in terms of ad delivery is that **No users will be excluded**. |
| `AUDIENCE_INCLUDE` | `AUDIENCE_INCLUDE` | `Array<unknown>` | No | No | Targeted customer list IDs. For example: ["2542620905473"]. Audience lists need to have at least 100 people with Pinterest accounts in them Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_INCLUDE field is missing, the default behavior in terms of ad delivery is that **All users will be included**. |
| `GENDER` | `GENDER` | `Array<string>` | No | No | Targeted genders. Values: ["unknown","male","female"]. If the GENDER field is missing, the default behavior in terms of ad delivery is that **All genders will be targeted**. |
| `GEO` | `GEO` | `Array<string>` | No | No | Region codes or postal codes to include for targeting. Region codes represent broader geographical areas. Example: `US-CA` is the region code for California in the United States. Postal codes represent more granular, specific areas. Example: `94103` is a postal code for a specifc area in San Francisco, California, U.S.A. For each ad group, use only one of these methods, depending on which fits your targeting needs. Do not use both. For example, either specify a broader region code like `US-CA` or a more granular postal code within that regon, such as `94103`. You can specify multiple region codes or postal codes in an array, depending on which method you choose. Precede a region code array with the `region_codes` key and a postal code value with the `postal_codes` key. Examples: ``` "geo": &#123; "region_codes": ["US-CA"] &#125; ``` ``` "geo": &#123; "postal_codes": ["94103"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION`. If you do not specifiy a `GEO` code, only `LOCATION` values will be targeted (See `LOCATION` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `GEO_EXCLUDE` | `GEO_EXCLUDE` | `Array<string>` | No | No | Closed beta Region codes or postal codes to exclude from the targeting inclusion area. See `GEO` parameter in this targeting spec for rules, syntax, and other information. |
| `INTEREST` | `INTEREST` | `Array<string>` | No | No | Array of interest object IDs. If the INTEREST field is missing, the default behavior in terms of ad delivery is that **All interests will be targeted**. |
| `LOCALE` | `LOCALE` | `Array<string>` | No | No | 24 ISO 639-1 two-letter language codes. If the LOCALE field is not included in the request, all languages are targeted. |
| `LOCATION` | `LOCATION` | `Array<string>` | No | No | Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to include for targeting. Precede country code values with the `country_codes` key and metro code values with `metro_codes` key. Example: ``` "location": &#123; "country_codes": ["US", "CA"], "metro_codes": ["501", "602"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION` code. If you do not specify a `LOCATION` code, only `GEO` values will be targeted (See `GEO` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `LOCATION_EXCLUDE` | `LOCATION_EXCLUDE` | `Array<string>` | No | No | Closed beta Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to exclude from targeting. See `LOCATION` parameter in this targeting spec for rules, syntax, and other information. |
| `MAXIMUM_AGE` | `MAXIMUM_AGE` | `string` | No | No | Maximum age to target (inclusive). Values: "18", "19", ..., "65", "65+". Must be used together with `MINIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `MINIMUM_AGE` | `MINIMUM_AGE` | `string` | No | No | Minimum age to target (inclusive). Values: "18", "19", ..., "65". Note: 65+ is not allowed for minimum age. Must be used together with `MAXIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `SHOPPING_RETARGETING` | `SHOPPING_RETARGETING` | `Array<Inline7384>` | No | No | Array of object: lookback_window [Integer]: Number of days ago to start lookback timeframe for dynamic retargeting tag_types [Array of integer]: Event types to target for dynamic retargeting exclusion_window [Integer]: Number of days ago to stop lookback timeframe for dynamic retargeting |
| `TARGETING_STRATEGY` | `TARGETING_STRATEGY` | `Array<string>` | No | No | — |

## Inline7385

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7386

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7387

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7388

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7389

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline739

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `BRAND` | `BRAND` | `Inline739` | Yes | No | — |

## Inline7390

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7391

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7392

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7393

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7394

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7395

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<string>` | Yes | No | — |

## Inline7396

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `values` | `values` | `Array<Inline7396>` | Yes | No | — |

## Inline7397

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `value` | `value` | `string` | Yes | No | — |

## Inline7398

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `field` | `field` | `string` | Yes | No | — |
| `operation` | `operation` | `string` | Yes | No | — |
| `value` | `value` | `string` | Yes | No | — |

## Inline7399

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline740

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CONDITION` | `CONDITION` | `Inline740` | Yes | No | — |

## Inline7401

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `items` | `items` | `Array<Inline7401>` | Yes | No | — |

## Inline7402

`object` model on the `json` wire format.

No fields are declared.

## Inline7403

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `enabled` | `enabled` | `Array<string>` | No | No | Tracking features. To clear this field, set to null. |

## Inline7404

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `conversion_tag_v3_goal_metadata` | `conversion_tag_v3_goal_metadata` | `Inline7404` | No | No | — |
| `frequency_goal_metadata` | `frequency_goal_metadata` | `Inline7405` | No | No | Frequency target can only be between 2 and 20 |
| `scrollup_goal_metadata` | `scrollup_goal_metadata` | `Inline7406` | No | No | — |

## Inline7405

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `frequency` | `frequency` | `number` | No | No | — |
| `timerange` | `timerange` | `string` | No | No | — |

## Inline7406

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `scrollup_goal_value_in_micro_currency` | `scrollup_goal_value_in_micro_currency` | `string` | No | No | — |

## Inline7407

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `boost_prospecting_ad_group_bid` | `boost_prospecting_ad_group_bid` | `boolean` | No | No | Whether to boost prospecting ad group bid. |
| `pinner_list_exclusions` | `pinner_list_exclusions` | `Array<string>` | No | No | List of campaign-level exclusion pinner list IDs. |

## Inline7408

`object` model on the `json` wire format.

No fields are declared.

## Inline7409

`object` model on the `json` wire format.

No fields are declared.

## Inline741

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_0` | `CUSTOM_LABEL_0` | `Inline741` | Yes | No | — |

## Inline7410

`object` model on the `json` wire format.

No fields are declared.

## Inline7411

`object` model on the `json` wire format.

No fields are declared.

## Inline7412

`object` model on the `json` wire format.

No fields are declared.

## Inline7413

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AGE_BUCKET` | `AGE_BUCKET` | `Array<string>` | No | No | **Legacy field.** Predefined age ranges. We recommend using MINIMUM_AGE and MAXIMUM_AGE instead for more flexible targeting. Cannot be combined with MINIMUM_AGE/MAXIMUM_AGE. If neither AGE_BUCKET nor MINIMUM_AGE/MAXIMUM_AGE are specified, all ages will be targeted. |
| `APPTYPE` | `APPTYPE` | `Array<string>` | No | No | Allowed devices. If the APPTYPE field is missing, the default behavior in terms of ad delivery is that **All devices/apptypes** will be targeted. |
| `AUDIENCE_EXCLUDE` | `AUDIENCE_EXCLUDE` | `Array<unknown>` | No | No | Excluded customer list IDs. Used to drive new customer acquisition goals. For example: ["2542620905475"]. Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_EXCLUDE field is missing, the default behavior in terms of ad delivery is that **No users will be excluded**. |
| `AUDIENCE_INCLUDE` | `AUDIENCE_INCLUDE` | `Array<unknown>` | No | No | Targeted customer list IDs. For example: ["2542620905473"]. Audience lists need to have at least 100 people with Pinterest accounts in them Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_INCLUDE field is missing, the default behavior in terms of ad delivery is that **All users will be included**. |
| `GENDER` | `GENDER` | `Array<string>` | No | No | Targeted genders. Values: ["unknown","male","female"]. If the GENDER field is missing, the default behavior in terms of ad delivery is that **All genders will be targeted**. |
| `GEO` | `GEO` | `Array<string>` | No | No | Region codes or postal codes to include for targeting. Region codes represent broader geographical areas. Example: `US-CA` is the region code for California in the United States. Postal codes represent more granular, specific areas. Example: `94103` is a postal code for a specifc area in San Francisco, California, U.S.A. For each ad group, use only one of these methods, depending on which fits your targeting needs. Do not use both. For example, either specify a broader region code like `US-CA` or a more granular postal code within that regon, such as `94103`. You can specify multiple region codes or postal codes in an array, depending on which method you choose. Precede a region code array with the `region_codes` key and a postal code value with the `postal_codes` key. Examples: ``` "geo": &#123; "region_codes": ["US-CA"] &#125; ``` ``` "geo": &#123; "postal_codes": ["94103"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION`. If you do not specifiy a `GEO` code, only `LOCATION` values will be targeted (See `LOCATION` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `GEO_EXCLUDE` | `GEO_EXCLUDE` | `Array<string>` | No | No | Closed beta Region codes or postal codes to exclude from the targeting inclusion area. See `GEO` parameter in this targeting spec for rules, syntax, and other information. |
| `INTEREST` | `INTEREST` | `Array<string>` | No | No | Array of interest object IDs. If the INTEREST field is missing, the default behavior in terms of ad delivery is that **All interests will be targeted**. |
| `LOCALE` | `LOCALE` | `Array<string>` | No | No | 24 ISO 639-1 two-letter language codes. If the LOCALE field is not included in the request, all languages are targeted. |
| `LOCATION` | `LOCATION` | `Array<string>` | No | No | Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to include for targeting. Precede country code values with the `country_codes` key and metro code values with `metro_codes` key. Example: ``` "location": &#123; "country_codes": ["US", "CA"], "metro_codes": ["501", "602"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION` code. If you do not specify a `LOCATION` code, only `GEO` values will be targeted (See `GEO` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `LOCATION_EXCLUDE` | `LOCATION_EXCLUDE` | `Array<string>` | No | No | Closed beta Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to exclude from targeting. See `LOCATION` parameter in this targeting spec for rules, syntax, and other information. |
| `MAXIMUM_AGE` | `MAXIMUM_AGE` | `string` | No | No | Maximum age to target (inclusive). Values: "18", "19", ..., "65", "65+". Must be used together with `MINIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `MINIMUM_AGE` | `MINIMUM_AGE` | `string` | No | No | Minimum age to target (inclusive). Values: "18", "19", ..., "65". Note: 65+ is not allowed for minimum age. Must be used together with `MAXIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `SHOPPING_RETARGETING` | `SHOPPING_RETARGETING` | `Array<Inline7413>` | No | No | Array of object: lookback_window [Integer]: Number of days ago to start lookback timeframe for dynamic retargeting tag_types [Array of integer]: Event types to target for dynamic retargeting exclusion_window [Integer]: Number of days ago to stop lookback timeframe for dynamic retargeting |
| `TARGETING_STRATEGY` | `TARGETING_STRATEGY` | `Array<string>` | No | No | — |

## Inline7414

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline7417

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `auto_targeting_enabled` | `auto_targeting_enabled` | `boolean` | No | No | Enable auto-targeting for ad group. Default value is True. Also known as ["Performance+ targeting"](https://help.pinterest.com/en/business/article/performance-plus-targeting). |
| `bid_multiplier` | `bid_multiplier` | `number` | No | No | [Open beta](https://help.pinterest.com/en/business/article/beta-features) Bid multiplier for ad group. This value is a double between 0.1 and 10.0. Enter 0 to remove the bid multiplier. - Not currently supported for [Performance+ campaigns](https://help.pinterest.com/en/business/article/performance-plus-campaigns). |
| `budget_type` | `budget_type` | `string` | No | No | Budget type. If DAILY, an ad group's daily spend will not exceed the budget parameter value. If LIFETIME, the end_time parameter is **REQUIRED**, and the ad group spend is spread evenly between the ad group `start_time` and `end_time` range. A CBO campaign automatically generates ad group budgets from its campaign budget to maximize campaign outcome. For CBO campaigns, only "CBO_ADGROUP" is allowed. For WEB_SESSIONS campaigns, only "LIFETIME" is allowed. For update, only draft ad groups may update budget type. |
| `pacing_delivery_type` | `pacing_delivery_type` | `string` | No | No | Ad group pacing delivery type. With ACCELERATED, an ad group budget is spent as fast as possible. With STANDARD, an ad group budget is spent smoothly over a day. When using CBO, only the STANDARD pacing delivery type is allowed. |
| `ad_account_id` | `ad_account_id` | `string` | No | No | Advertiser ID. |
| `bid_in_micro_currency` | `bid_in_micro_currency` | `number` | No | No | Bid price in micro currency. This field is **REQUIRED** for the following campaign objective_type/billable_event combinations: AWARENESS/IMPRESSION, CONSIDERATION/CLICKTHROUGH, CATALOG_SALES/CLICKTHROUGH, VIDEO_VIEW/VIDEO_V_50_MRC. |
| `bid_strategy_type` | `bid_strategy_type` | `string` | No | No | Bid strategy type. For Campaigns with Video Completion objectives, the only supported bid strategy type is AUTOMATIC_BID, also known as "Pinterest Performance+ bidding". |
| `billable_event` | `billable_event` | `string` | No | No | Ad group billable event type. For update, only draft ad groups may update billable event. |
| `budget_in_micro_currency` | `budget_in_micro_currency` | `number` | No | No | Budget in micro currency. This field is **REQUIRED** for non-CBO (campaign budget optimization) campaigns. A CBO campaign automatically generates ad group budgets from its campaign budget to maximize campaign outcome. A CBO campaign is limited to 70 or less ad groups. |
| `campaign_id` | `campaign_id` | `string` | No | No | Campaign ID of the ad group. |
| `conversion_learning_mode_type` | `conversion_learning_mode_type` | `Inline7417` | No | No | oCPM learn mode |
| `created_time` | `created_time` | `number` | No | No | Ad group creation time. Unix timestamp in seconds. |
| `customer_segment_id` | `customer_segment_id` | `Inline7418` | No | No | Customer segment ID applied to the ad group. We currently only support 1 customer segment per ad group. To use customer segments, do not set the `targeting_template_ids` field as well as the `AUDIENCE_INCLUDE` field of the `targeting_spec`. To clear the customer segment ID, set this field to '0'. |
| `dca_assets` | `dca_assets` | `unknown` | No | No | [DCA] The Dynamic creative assets to use for DCA. Dynamic Creative Assembly (DCA) accepts basic creative assets of an ad (image, video, title, call to action, logo etc). Then it automatically generates optimized ad combinations based on these assets. |
| `end_time` | `end_time` | `number` | No | No | Timestamp in Unix format for scheduling when ads in the ad group stop appearing. If not specified, ads run indefinitely unless you update the ad group by changing their status to `paused`. Cannot occur after `end_time` for parent campaign (if specified). Learn about [scheduling ads](https://help.pinterest.com/en/business/article/ads-manager-schedule-ads). For certain organizations ([Closed beta](https://help.pinterest.com/en/business/article/beta-features)): Supported for campaigns with Campaign Budget Optimization (CBO). For all organizations: Supported for campaigns without CBO. |
| `ext_features` | `ext_features` | `Inline7419` | No | No | — |
| `feed_profile_id` | `feed_profile_id` | `string` | No | No | Feed Profile ID associated to the adgroup. |
| `id` | `id` | `string` | No | No | Ad group ID. |
| `is_creative_optimization` | `is_creative_optimization` | `boolean` | No | No | Enable creative optimization for the ad group, default value is FALSE. When enabled, you allow Pinterest to automatically turn your product Pins into ads in different formats (collections and shopping) and deliver those ads to users at scale. |
| `is_local_inventory` | `is_local_inventory` | `boolean` | No | No | Indicates whether the ad group should use the local inventory. |
| `lifetime_frequency_cap` | `lifetime_frequency_cap` | `number` | No | No | Set a limit to the number of times a promoted pin from this campaign can be impressed by a pinner within the past rolling 30 days. Only available for CPM (cost per mille (1000 impressions)) ad groups. A CPM ad group has an IMPRESSION [billable_event](https://help.pinterest.com/en/business/article/billable-events) value. This field **REQUIRES** the `end_time` field. |
| `local_inventory_radius_in_miles` | `local_inventory_radius_in_miles` | `number` | No | No | The targeting radius of the local inventory ads in miles. |
| `name` | `name` | `string` | No | No | Ad group name. |
| `optimization_goal_metadata` | `optimization_goal_metadata` | `Inline7420` | No | No | Optimization goals for objective-based performance campaigns. **REQUIRED** when campaign's `objective_type` is set to `"WEB_CONVERSION"`. |
| `performance_plus_campaign_settings` | `performance_plus_campaign_settings` | `Inline7423` | No | No | Pinterest Performance+ campaign settings. |
| `placement_group` | `placement_group` | `Inline7424` | No | No | [Placement group](https://help.pinterest.com/en/business/article/placement-groups). |
| `placement_traffic_type` | `placement_traffic_type` | `Inline7425` | No | No | A targeting option that enables advertisers to choose whether to run ads in fullscreen feed, two column feed, or both |
| `promotion_application_level` | `promotion_application_level` | `Inline7426` | No | No | Specify if the promotion is applied at ad group or item level |
| `promotion_id` | `promotion_id` | `string` | No | No | Promotion ID. To clear this field, set to null. |
| `promotion_ids` | `promotion_ids` | `Array<string>` | No | No | Promotion IDs list. To clear this field, set to an empty array []. |
| `start_time` | `start_time` | `number` | No | No | Timestamp in Unix format for scheduling when ads in the ad group start to appear. If not specified, ads appear during parent campaign's `start_time`. Cannot precede `start_time` for parent campaign (if specified). Learn about scheduling ads. For certain organizations (Closed beta): Supported for campaigns with Campaign Budget Optimization (CBO). For all organizations: Supported for campaigns without CBO. |
| `status` | `status` | `Inline7427` | No | No | Ad group/entity status. |
| `summary_status` | `summary_status` | `Inline7428` | No | No | — |
| `targeting_spec` | `targeting_spec` | `Inline7429` | No | No | — |
| `targeting_template_ids` | `targeting_template_ids` | `Array<unknown>` | No | No | Targeting template IDs applied to the ad group. We currently only support 1 targeting template per ad group. To use targeting templates, do not set any other targeting fields: targeting_spec, tracking_urls, auto_targeting_enabled, placement_group. To clear all targeting template IDs, set this field to ['0']. |
| `tracking_urls` | `tracking_urls` | `Inline7430` | No | No | Third-party tracking URLs. Up to three tracking URLs - with a max length of 2,000 - are supported for each event type. Tracking URLs set at the ad group or ad level can override those set at the campaign level. For more information, see [Third-party and dynamic tracking](https://help.pinterest.com/en/business/article/third-party-and-dynamic-tracking). |
| `type` | `type` | `string` | No | No | Always "adgroup". |
| `updated_time` | `updated_time` | `number` | No | No | Ad group last update time. Unix timestamp in seconds. |

## Inline7418

`object` model on the `json` wire format.

No fields are declared.

## Inline7419

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `enabled` | `enabled` | `Array<string>` | No | No | Tracking features. To clear this field, set to null. |

## Inline742

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_1` | `CUSTOM_LABEL_1` | `Inline742` | Yes | No | — |

## Inline7420

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `conversion_tag_v3_goal_metadata` | `conversion_tag_v3_goal_metadata` | `Inline7420` | No | No | — |
| `frequency_goal_metadata` | `frequency_goal_metadata` | `Inline7421` | No | No | Frequency target can only be between 2 and 20 |
| `scrollup_goal_metadata` | `scrollup_goal_metadata` | `Inline7422` | No | No | — |

## Inline7421

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `frequency` | `frequency` | `number` | No | No | — |
| `timerange` | `timerange` | `string` | No | No | — |

## Inline7422

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `scrollup_goal_value_in_micro_currency` | `scrollup_goal_value_in_micro_currency` | `string` | No | No | — |

## Inline7423

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `boost_prospecting_ad_group_bid` | `boost_prospecting_ad_group_bid` | `boolean` | No | No | Whether to boost prospecting ad group bid. |
| `pinner_list_exclusions` | `pinner_list_exclusions` | `Array<string>` | No | No | List of campaign-level exclusion pinner list IDs. |

## Inline7424

`object` model on the `json` wire format.

No fields are declared.

## Inline7425

`object` model on the `json` wire format.

No fields are declared.

## Inline7426

`object` model on the `json` wire format.

No fields are declared.

## Inline7427

`object` model on the `json` wire format.

No fields are declared.

## Inline7428

`object` model on the `json` wire format.

No fields are declared.

## Inline7429

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AGE_BUCKET` | `AGE_BUCKET` | `Array<string>` | No | No | **Legacy field.** Predefined age ranges. We recommend using MINIMUM_AGE and MAXIMUM_AGE instead for more flexible targeting. Cannot be combined with MINIMUM_AGE/MAXIMUM_AGE. If neither AGE_BUCKET nor MINIMUM_AGE/MAXIMUM_AGE are specified, all ages will be targeted. |
| `APPTYPE` | `APPTYPE` | `Array<string>` | No | No | Allowed devices. If the APPTYPE field is missing, the default behavior in terms of ad delivery is that **All devices/apptypes** will be targeted. |
| `AUDIENCE_EXCLUDE` | `AUDIENCE_EXCLUDE` | `Array<unknown>` | No | No | Excluded customer list IDs. Used to drive new customer acquisition goals. For example: ["2542620905475"]. Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_EXCLUDE field is missing, the default behavior in terms of ad delivery is that **No users will be excluded**. |
| `AUDIENCE_INCLUDE` | `AUDIENCE_INCLUDE` | `Array<unknown>` | No | No | Targeted customer list IDs. For example: ["2542620905473"]. Audience lists need to have at least 100 people with Pinterest accounts in them Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_INCLUDE field is missing, the default behavior in terms of ad delivery is that **All users will be included**. |
| `GENDER` | `GENDER` | `Array<string>` | No | No | Targeted genders. Values: ["unknown","male","female"]. If the GENDER field is missing, the default behavior in terms of ad delivery is that **All genders will be targeted**. |
| `GEO` | `GEO` | `Array<string>` | No | No | Region codes or postal codes to include for targeting. Region codes represent broader geographical areas. Example: `US-CA` is the region code for California in the United States. Postal codes represent more granular, specific areas. Example: `94103` is a postal code for a specifc area in San Francisco, California, U.S.A. For each ad group, use only one of these methods, depending on which fits your targeting needs. Do not use both. For example, either specify a broader region code like `US-CA` or a more granular postal code within that regon, such as `94103`. You can specify multiple region codes or postal codes in an array, depending on which method you choose. Precede a region code array with the `region_codes` key and a postal code value with the `postal_codes` key. Examples: ``` "geo": &#123; "region_codes": ["US-CA"] &#125; ``` ``` "geo": &#123; "postal_codes": ["94103"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION`. If you do not specifiy a `GEO` code, only `LOCATION` values will be targeted (See `LOCATION` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `GEO_EXCLUDE` | `GEO_EXCLUDE` | `Array<string>` | No | No | Closed beta Region codes or postal codes to exclude from the targeting inclusion area. See `GEO` parameter in this targeting spec for rules, syntax, and other information. |
| `INTEREST` | `INTEREST` | `Array<string>` | No | No | Array of interest object IDs. If the INTEREST field is missing, the default behavior in terms of ad delivery is that **All interests will be targeted**. |
| `LOCALE` | `LOCALE` | `Array<string>` | No | No | 24 ISO 639-1 two-letter language codes. If the LOCALE field is not included in the request, all languages are targeted. |
| `LOCATION` | `LOCATION` | `Array<string>` | No | No | Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to include for targeting. Precede country code values with the `country_codes` key and metro code values with `metro_codes` key. Example: ``` "location": &#123; "country_codes": ["US", "CA"], "metro_codes": ["501", "602"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION` code. If you do not specify a `LOCATION` code, only `GEO` values will be targeted (See `GEO` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `LOCATION_EXCLUDE` | `LOCATION_EXCLUDE` | `Array<string>` | No | No | Closed beta Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to exclude from targeting. See `LOCATION` parameter in this targeting spec for rules, syntax, and other information. |
| `MAXIMUM_AGE` | `MAXIMUM_AGE` | `string` | No | No | Maximum age to target (inclusive). Values: "18", "19", ..., "65", "65+". Must be used together with `MINIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `MINIMUM_AGE` | `MINIMUM_AGE` | `string` | No | No | Minimum age to target (inclusive). Values: "18", "19", ..., "65". Note: 65+ is not allowed for minimum age. Must be used together with `MAXIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `SHOPPING_RETARGETING` | `SHOPPING_RETARGETING` | `Array<Inline7429>` | No | No | Array of object: lookback_window [Integer]: Number of days ago to start lookback timeframe for dynamic retargeting tag_types [Array of integer]: Event types to target for dynamic retargeting exclusion_window [Integer]: Number of days ago to stop lookback timeframe for dynamic retargeting |
| `TARGETING_STRATEGY` | `TARGETING_STRATEGY` | `Array<string>` | No | No | — |

## Inline743

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_2` | `CUSTOM_LABEL_2` | `Inline743` | Yes | No | — |

## Inline7430

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline7433

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `errors` | `errors` | `Array<Inline7433>` | No | No | List of validation errors. Empty on success. |
| `status` | `status` | `string` | No | No | Processing status. Present on success. |

## Inline7436

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `download_url` | `download_url` | `string` | No | No | Pre-signed S3 URL to download the CSV file. |

## Inline7439

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `generated_count` | `generated_count` | `number` | No | No | The count of generated titles. |
| `is_ready` | `is_ready` | `boolean` | No | No | Whether dynamic titles have been generated and are ready to be reviewed for the ad group. |
| `reviewed_count` | `reviewed_count` | `number` | No | No | The count of advertiser reviewed titles. |

## Inline744

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_3` | `CUSTOM_LABEL_3` | `Inline744` | Yes | No | — |

## Inline7442

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `existing_filename` | `existing_filename` | `string` | No | No | If present, a reviewed CSV was previously submitted for this ad group. Uploading a new file will replace it. Absent when no prior submission exists. |
| `request_id` | `request_id` | `string` | Yes | No | Unique identifier for this upload session. Must be passed to the process endpoint. |
| `upload_url` | `upload_url` | `string` | Yes | No | Pre-signed S3 PUT URL to upload the reviewed CSV file. |

## Inline7445

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AD_GROUP_ID` | `AD_GROUP_ID` | `string` | No | No | The ID of the ad group that this metrics belongs to. Returned as long as aggregate_report_rows is not true. |
| `DATE` | `DATE` | `string` | No | No | Current metrics date. Only returned when granularity is a time-based value (`DAY`, `HOUR`, `WEEK`, `MONTH`) |

## Inline7447

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `match_type` | `match_type` | `string` | Yes | No | Keyword match type |
| `value` | `value` | `string` | Yes | No | Keyword value (120 chars max). |

## Inline7448

`object` model on the `json` wire format.

No fields are declared.

## Inline7449

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AGE_BUCKET` | `AGE_BUCKET` | `Array<string>` | No | No | **Legacy field.** Predefined age ranges. We recommend using MINIMUM_AGE and MAXIMUM_AGE instead for more flexible targeting. Cannot be combined with MINIMUM_AGE/MAXIMUM_AGE. If neither AGE_BUCKET nor MINIMUM_AGE/MAXIMUM_AGE are specified, all ages will be targeted. |
| `APPTYPE` | `APPTYPE` | `Array<string>` | No | No | Allowed devices. If the APPTYPE field is missing, the default behavior in terms of ad delivery is that **All devices/apptypes** will be targeted. |
| `AUDIENCE_EXCLUDE` | `AUDIENCE_EXCLUDE` | `Array<unknown>` | No | No | Excluded customer list IDs. Used to drive new customer acquisition goals. For example: ["2542620905475"]. Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_EXCLUDE field is missing, the default behavior in terms of ad delivery is that **No users will be excluded**. |
| `AUDIENCE_INCLUDE` | `AUDIENCE_INCLUDE` | `Array<unknown>` | No | No | Targeted customer list IDs. For example: ["2542620905473"]. Audience lists need to have at least 100 people with Pinterest accounts in them Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_INCLUDE field is missing, the default behavior in terms of ad delivery is that **All users will be included**. |
| `GENDER` | `GENDER` | `Array<string>` | No | No | Targeted genders. Values: ["unknown","male","female"]. If the GENDER field is missing, the default behavior in terms of ad delivery is that **All genders will be targeted**. |
| `GEO` | `GEO` | `Array<string>` | No | No | Region codes or postal codes to include for targeting. Region codes represent broader geographical areas. Example: `US-CA` is the region code for California in the United States. Postal codes represent more granular, specific areas. Example: `94103` is a postal code for a specifc area in San Francisco, California, U.S.A. For each ad group, use only one of these methods, depending on which fits your targeting needs. Do not use both. For example, either specify a broader region code like `US-CA` or a more granular postal code within that regon, such as `94103`. You can specify multiple region codes or postal codes in an array, depending on which method you choose. Precede a region code array with the `region_codes` key and a postal code value with the `postal_codes` key. Examples: ``` "geo": &#123; "region_codes": ["US-CA"] &#125; ``` ``` "geo": &#123; "postal_codes": ["94103"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION`. If you do not specifiy a `GEO` code, only `LOCATION` values will be targeted (See `LOCATION` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `GEO_EXCLUDE` | `GEO_EXCLUDE` | `Array<string>` | No | No | Closed beta Region codes or postal codes to exclude from the targeting inclusion area. See `GEO` parameter in this targeting spec for rules, syntax, and other information. |
| `INTEREST` | `INTEREST` | `Array<string>` | No | No | Array of interest object IDs. If the INTEREST field is missing, the default behavior in terms of ad delivery is that **All interests will be targeted**. |
| `LOCALE` | `LOCALE` | `Array<string>` | No | No | 24 ISO 639-1 two-letter language codes. If the LOCALE field is not included in the request, all languages are targeted. |
| `LOCATION` | `LOCATION` | `Array<string>` | No | No | Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to include for targeting. Precede country code values with the `country_codes` key and metro code values with `metro_codes` key. Example: ``` "location": &#123; "country_codes": ["US", "CA"], "metro_codes": ["501", "602"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION` code. If you do not specify a `LOCATION` code, only `GEO` values will be targeted (See `GEO` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `LOCATION_EXCLUDE` | `LOCATION_EXCLUDE` | `Array<string>` | No | No | Closed beta Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to exclude from targeting. See `LOCATION` parameter in this targeting spec for rules, syntax, and other information. |
| `MAXIMUM_AGE` | `MAXIMUM_AGE` | `string` | No | No | Maximum age to target (inclusive). Values: "18", "19", ..., "65", "65+". Must be used together with `MINIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `MINIMUM_AGE` | `MINIMUM_AGE` | `string` | No | No | Minimum age to target (inclusive). Values: "18", "19", ..., "65". Note: 65+ is not allowed for minimum age. Must be used together with `MAXIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `SHOPPING_RETARGETING` | `SHOPPING_RETARGETING` | `Array<Inline7449>` | No | No | Array of object: lookback_window [Integer]: Number of days ago to start lookback timeframe for dynamic retargeting tag_types [Array of integer]: Event types to target for dynamic retargeting exclusion_window [Integer]: Number of days ago to stop lookback timeframe for dynamic retargeting |
| `TARGETING_STRATEGY` | `TARGETING_STRATEGY` | `Array<string>` | No | No | — |

## Inline745

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_4` | `CUSTOM_LABEL_4` | `Inline745` | Yes | No | — |

## Inline7451

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_size_lower_bound` | `audience_size_lower_bound` | `number` | No | No | The lower confidence bound of the estimated potential audience size. "Potential audience size" estimates the number of people you may be able to reach per month with your campaign. It is based on historical advertising data and the targeting criteria you select. It does not guarantee results or take into account factors such as bid, budget, schedule, seasonality or product experiments. |
| `audience_size_upper_bound` | `audience_size_upper_bound` | `number` | No | No | The upper confidence bound of the estimated potential audience size. "Potential audience size" estimates the number of people you may be able to reach per month with your campaign. It is based on historical advertising data and the targeting criteria you select. It does not guarantee results or take into account factors such as bid, budget, schedule, seasonality or product experiments. |

## Inline7453

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `match_type` | `match_type` | `string` | Yes | No | Keyword match type |
| `value` | `value` | `string` | Yes | No | Keyword value (120 chars max). |

## Inline7454

`object` model on the `json` wire format.

No fields are declared.

## Inline7455

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AGE_BUCKET` | `AGE_BUCKET` | `Array<string>` | No | No | **Legacy field.** Predefined age ranges. We recommend using MINIMUM_AGE and MAXIMUM_AGE instead for more flexible targeting. Cannot be combined with MINIMUM_AGE/MAXIMUM_AGE. If neither AGE_BUCKET nor MINIMUM_AGE/MAXIMUM_AGE are specified, all ages will be targeted. |
| `APPTYPE` | `APPTYPE` | `Array<string>` | No | No | Allowed devices. If the APPTYPE field is missing, the default behavior in terms of ad delivery is that **All devices/apptypes** will be targeted. |
| `AUDIENCE_EXCLUDE` | `AUDIENCE_EXCLUDE` | `Array<unknown>` | No | No | Excluded customer list IDs. Used to drive new customer acquisition goals. For example: ["2542620905475"]. Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_EXCLUDE field is missing, the default behavior in terms of ad delivery is that **No users will be excluded**. |
| `AUDIENCE_INCLUDE` | `AUDIENCE_INCLUDE` | `Array<unknown>` | No | No | Targeted customer list IDs. For example: ["2542620905473"]. Audience lists need to have at least 100 people with Pinterest accounts in them Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_INCLUDE field is missing, the default behavior in terms of ad delivery is that **All users will be included**. |
| `GENDER` | `GENDER` | `Array<string>` | No | No | Targeted genders. Values: ["unknown","male","female"]. If the GENDER field is missing, the default behavior in terms of ad delivery is that **All genders will be targeted**. |
| `GEO` | `GEO` | `Array<string>` | No | No | Region codes or postal codes to include for targeting. Region codes represent broader geographical areas. Example: `US-CA` is the region code for California in the United States. Postal codes represent more granular, specific areas. Example: `94103` is a postal code for a specifc area in San Francisco, California, U.S.A. For each ad group, use only one of these methods, depending on which fits your targeting needs. Do not use both. For example, either specify a broader region code like `US-CA` or a more granular postal code within that regon, such as `94103`. You can specify multiple region codes or postal codes in an array, depending on which method you choose. Precede a region code array with the `region_codes` key and a postal code value with the `postal_codes` key. Examples: ``` "geo": &#123; "region_codes": ["US-CA"] &#125; ``` ``` "geo": &#123; "postal_codes": ["94103"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION`. If you do not specifiy a `GEO` code, only `LOCATION` values will be targeted (See `LOCATION` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `GEO_EXCLUDE` | `GEO_EXCLUDE` | `Array<string>` | No | No | Closed beta Region codes or postal codes to exclude from the targeting inclusion area. See `GEO` parameter in this targeting spec for rules, syntax, and other information. |
| `INTEREST` | `INTEREST` | `Array<string>` | No | No | Array of interest object IDs. If the INTEREST field is missing, the default behavior in terms of ad delivery is that **All interests will be targeted**. |
| `LOCALE` | `LOCALE` | `Array<string>` | No | No | 24 ISO 639-1 two-letter language codes. If the LOCALE field is not included in the request, all languages are targeted. |
| `LOCATION` | `LOCATION` | `Array<string>` | No | No | Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to include for targeting. Precede country code values with the `country_codes` key and metro code values with `metro_codes` key. Example: ``` "location": &#123; "country_codes": ["US", "CA"], "metro_codes": ["501", "602"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION` code. If you do not specify a `LOCATION` code, only `GEO` values will be targeted (See `GEO` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `LOCATION_EXCLUDE` | `LOCATION_EXCLUDE` | `Array<string>` | No | No | Closed beta Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to exclude from targeting. See `LOCATION` parameter in this targeting spec for rules, syntax, and other information. |
| `MAXIMUM_AGE` | `MAXIMUM_AGE` | `string` | No | No | Maximum age to target (inclusive). Values: "18", "19", ..., "65", "65+". Must be used together with `MINIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `MINIMUM_AGE` | `MINIMUM_AGE` | `string` | No | No | Minimum age to target (inclusive). Values: "18", "19", ..., "65". Note: 65+ is not allowed for minimum age. Must be used together with `MAXIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `SHOPPING_RETARGETING` | `SHOPPING_RETARGETING` | `Array<Inline7455>` | No | No | Array of object: lookback_window [Integer]: Number of days ago to start lookback timeframe for dynamic retargeting tag_types [Array of integer]: Event types to target for dynamic retargeting exclusion_window [Integer]: Number of days ago to stop lookback timeframe for dynamic retargeting |
| `TARGETING_STRATEGY` | `TARGETING_STRATEGY` | `Array<string>` | No | No | — |

## Inline7457

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `data` | `data` | `Array<Inline7457>` | No | No | — |

## Inline7459

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `image_url` | `image_url` | `string` | Yes | No | Image URL. |
| `promotion_id` | `promotion_id` | `string` | No | No | Promotion id for the ad to preview, optional and only applicable when creating ad preview for an existing promotion. |
| `title` | `title` | `string` | Yes | No | Title displayed below ad. |

## Inline746

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `ITEM_GROUP_ID` | `ITEM_GROUP_ID` | `Inline746` | Yes | No | — |

## Inline7460

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `creative_type` | `creative_type` | `Inline7460` | No | No | Creative type of the ad preview. |
| `pin_id` | `pin_id` | `string` | Yes | No | Pin ID. |

## Inline7461

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `catalog_product_group_id` | `catalog_product_group_id` | `string` | Yes | No | Catalog Product Group Id. |
| `creative_type` | `creative_type` | `Inline7461` | Yes | No | Ad format of the shopping ad preview. |
| `customizable_cta_type` | `customizable_cta_type` | `Inline7462` | No | No | Select a call to action (CTA) to display below your ad. CTA options for catalog sales campaigns are `SHOP_NOW`, `BOOK_NOW`, `ON_SALE`, `GET_DEAL`, `BUY_ONLINE_PICKUP_IN_STORE` |
| `hero_image_title` | `hero_image_title` | `string` | No | No | Title displayed below ad. |
| `hero_image_url` | `hero_image_url` | `string` | No | No | Hero image URL. |
| `hero_pin_id` | `hero_pin_id` | `string` | No | No | Pin id for the hero image. When creative type is COLLECTION, either hero_pin_id or (hero_image_url, hero_image_title) is required. |
| `image_tag` | `image_tag` | `string` | No | No | Multi image template tag. |
| `item_id` | `item_id` | `string` | No | No | Item id for product to preview standard shopping ads, optional and only applicable when creative type is SHOPPING. |
| `preferred_media_type` | `preferred_media_type` | `Inline7463` | No | No | Preferred media type. |
| `show_promotion` | `show_promotion` | `boolean` | No | No | Include promotion data in preview when available on catalog item. Defaults to false. |
| `video_tag` | `video_tag` | `string` | No | No | Multi video template tag, image_tag and video_tag are mutual exclusive. |

## Inline7462

`object` model on the `json` wire format.

No fields are declared.

## Inline7463

`object` model on the `json` wire format.

No fields are declared.

## Inline7465

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `url` | `url` | `string` | No | No | Preview URL, expires in 7 days. Can be used in an iframe. For example: https://ads.pinterest.com/ad-preview/74667c814dd2b19/ The preview object ID/key is the last param - 74667c814dd2b19 |

## Inline7468

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `bookmark` | `bookmark` | `string` | No | No | — |
| `items` | `items` | `Array<Inline7468>` | Yes | No | — |

## Inline7469

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `carting_product_id` | `carting_product_id` | `string` | Yes | No | the internal Pinterest carting product id (different from industry product id) |
| `display_preferred_retailers_only` | `display_preferred_retailers_only` | `boolean` | No | No | Whether to display only the preferred retailers for the carting product |
| `display_product_price` | `display_product_price` | `boolean` | No | No | Whether to display the price for the carting product |
| `preferred_retailers` | `preferred_retailers` | `Array<Inline7469>` | No | No | A sorted array of preferred retailers for the carting product |
| `randomize_preferred_retailers` | `randomize_preferred_retailers` | `boolean` | No | No | Whether to randomize preferred retailers for the carting product |

## Inline747

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GENDER` | `GENDER` | `Inline747` | Yes | No | — |

## Inline7470

`object` model on the `json` wire format.

No fields are declared.

## Inline7471

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `questions` | `questions` | `Array<Inline7471>` | No | No | — |
| `results` | `results` | `Array<Inline7472>` | No | No | — |
| `tie_breaker_custom_result` | `tie_breaker_custom_result` | `Inline7473` | No | No | The result, and link out, based on the user’s choice. |
| `tie_breaker_type` | `tie_breaker_type` | `string` | No | No | Quiz ad tie breaker type, default is RANDOM |

## Inline7472

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `android_deep_link` | `android_deep_link` | `string` | No | No | — |
| `destination_url` | `destination_url` | `string` | No | No | — |
| `ios_deep_link` | `ios_deep_link` | `string` | No | No | — |
| `organic_pin_id` | `organic_pin_id` | `string` | No | No | — |
| `result_id` | `result_id` | `number` | No | No | — |

## Inline7473

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `android_deep_link` | `android_deep_link` | `string` | No | No | — |
| `destination_url` | `destination_url` | `string` | No | No | — |
| `ios_deep_link` | `ios_deep_link` | `string` | No | No | — |
| `organic_pin_id` | `organic_pin_id` | `string` | No | No | — |
| `result_id` | `result_id` | `number` | No | No | — |

## Inline7474

`object` model on the `json` wire format.

No fields are declared.

## Inline7475

`object` model on the `json` wire format.

No fields are declared.

## Inline7476

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline7478

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `ad_group_id` | `ad_group_id` | `string` | Yes | No | ID of the ad group that contains the ad. |
| `android_deep_link` | `android_deep_link` | `string` | No | No | Deep link URL for Android devices. |
| `carousel_android_deep_links` | `carousel_android_deep_links` | `Array<string>` | No | No | Comma-separated deep links for the carousel pin on Android. |
| `carousel_destination_urls` | `carousel_destination_urls` | `Array<string>` | No | No | Comma-separated destination URLs for the carousel pin to promote. |
| `carousel_ios_deep_links` | `carousel_ios_deep_links` | `Array<string>` | No | No | Comma-separated deep links for the carousel pin on iOS. |
| `click_tracking_url` | `click_tracking_url` | `string` | No | No | Tracking url for the ad clicks. |
| `collection_items_destination_url_template` | `collection_items_destination_url_template` | `string` | No | No | Destination URL template for all items within a collections drawer. |
| `collections_header_type` | `collections_header_type` | `Inline7478` | No | No | — |
| `creative_type` | `creative_type` | `string` | Yes | No | Ad creative type enum. **Note:** SHOP_THE_PIN has been deprecated. Please use COLLECTION instead. |
| `customizable_cta_type` | `customizable_cta_type` | `string` | No | No | Select a call to action (CTA) to display below your ad. Available only for ads with direct links enabled. CTA options for consideration and conversion campaigns are LEARN_MORE, SHOP_NOW, BOOK_NOW, SIGN_UP, VISIT_SITE, BUY_NOW, GET_OFFER, ORDER_NOW, ADD_TO_CART (for conversion campaigns with add to cart conversion events only) |
| `destination_url` | `destination_url` | `string` | No | No | Destination URL. |
| `disclosure_type` | `disclosure_type` | `string` | No | No | Type of information in the page referenced by `disclosure_url`, provided either by the Food and Drug Administration (FDA) or the manufacturer. |
| `disclosure_url` | `disclosure_url` | `string` | No | No | URL for a page that provides disclosures about a pharmaceutical product, such as potential side effects. Make sure the URL takes the user directly to the disclosure content and the referenced site is secure. |
| `grid_click_type` | `grid_click_type` | `string` | No | No | Where a user is taken after clicking on an ad in grid. **Note:** This parameter is read-only and is set to `DIRECT_TO_DESTINATION` by default for direct links supported ads. `grid_click_type` values provided will be ignored. |
| `ios_deep_link` | `ios_deep_link` | `string` | No | No | Deep link URL for iOS devices. |
| `is_carting` | `is_carting` | `boolean` | No | No | Is the ad a carting/WTB ad? |
| `is_collage_accepted_terms` | `is_collage_accepted_terms` | `boolean` | No | No | Whether the advertiser has accepted the terms and conditions for collage ad. |
| `is_collage_single_destination` | `is_collage_single_destination` | `boolean` | No | No | Whether the collage ad has a single destination url override. |
| `is_pin_deleted` | `is_pin_deleted` | `boolean` | No | No | Is original pin deleted? |
| `is_removable` | `is_removable` | `boolean` | No | No | Is pin repinnable? |
| `lead_form_id` | `lead_form_id` | `string` | No | No | Lead form ID for lead ad generation. |
| `name` | `name` | `string` | No | No | Name of the ad - 255 chars max. |
| `pin_id` | `pin_id` | `string` | Yes | No | Pin ID. This field may only be updated for draft ads. |
| `quiz_pin_data` | `quiz_pin_data` | `Inline7479` | No | No | Before creating a quiz ad, you must create an organic Pin using POST/Create Pin for each result in the quiz. Quiz ads cannot be saved by a Pinner. Quiz ad results can be saved. |
| `status` | `status` | `string` | No | No | Entity status |
| `tracking_urls` | `tracking_urls` | `Inline7482` | No | No | Third-party tracking URLs. Up to three tracking URLs - with a max length of 2,000 - are supported for each event type. Tracking URLs set at the ad group or ad level can override those set at the campaign level. For more information, see [Third-party and dynamic tracking](https://help.pinterest.com/en/business/article/third-party-and-dynamic-tracking). |
| `view_tracking_url` | `view_tracking_url` | `string` | No | No | Tracking URL for ad impressions. |

## Inline7479

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `questions` | `questions` | `Array<Inline7479>` | No | No | — |
| `results` | `results` | `Array<Inline7480>` | No | No | — |
| `tie_breaker_custom_result` | `tie_breaker_custom_result` | `Inline7481` | No | No | The result, and link out, based on the user’s choice. |
| `tie_breaker_type` | `tie_breaker_type` | `string` | No | No | Quiz ad tie breaker type, default is RANDOM |

## Inline748

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `MEDIA_TYPE` | `MEDIA_TYPE` | `Inline748` | Yes | No | — |

## Inline7480

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `android_deep_link` | `android_deep_link` | `string` | No | No | — |
| `destination_url` | `destination_url` | `string` | No | No | — |
| `ios_deep_link` | `ios_deep_link` | `string` | No | No | — |
| `organic_pin_id` | `organic_pin_id` | `string` | No | No | — |
| `result_id` | `result_id` | `number` | No | No | — |
