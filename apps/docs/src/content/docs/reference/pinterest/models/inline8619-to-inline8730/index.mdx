---
title: "Pinterest models: Inline8619 – Inline8730"
description: "100 generated Pinterest model contracts, from Inline8619 through Inline8730."
generated: true
platform: "pinterest"
sourceVersion: "v5"
sidebar:
  order: 1097
  label: "Inline8619 – Inline8730"
tableOfContents: false
---

<!-- @generated -->
<!-- fingerprint: sha256:bcdd4ae2de9d4dd118fbe6ce105b508d4e2f71292256f761861d473792661f7a -->
<!-- DO NOT EDIT: generated file; changes will be overwritten. -->

{/* Generated from canonical SdkIr (openapi: https://github.com/pinterest/api-description). Do not edit by hand. */}

## Inline8619

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `ad_account_id` | `ad_account_id` | `string` | No | No | Setting this field does nothing. The ad account ID gets set from the path parameter. |
| `app_id` | `app_id` | `string` | No | No | Closed beta The store ID of the mobile app for `APP_INSTALL` campaigns. |
| `app_platform` | `app_platform` | `Inline8619` | No | No | Closed beta Platform of the mobile app for `APP_INSTALL` campaigns. |
| `bid_options` | `bid_options` | `Inline8620` | No | No | Restricted Object describing the campaign level bid multipliers. |
| `daily_spend_cap` | `daily_spend_cap` | `number` | No | No | Note: The default for this field is null, which is treated the same as 0. For campaign budget optimization, a daily or lifetime spend cap must be set, but not both. For ad group level budgets, this field must be 0 or unset. |
| `default_ad_group_budget_in_micro_currency` | `default_ad_group_budget_in_micro_currency` | `number` | No | No | Setting this field does nothing. |
| `end_time` | `end_time` | `number` | No | No | Note: To clear this field on update, set the end time to -1. Timestamp in Unix format for scheduling when ads in the campaign stop appearing. Must occur after any end times for child ad groups. If `end_time` is not specified for the campaign, ads run indefinitely unless you update the campaign, changing their status to `paused`. Learn about scheduling campaigns. Different end times can be set for the campaign's child ad groups, but they cannot occur after an `end_time` specified for the campaign. - If your campaign has a child ad group with an end time specified, and if you update that campaign with an `end_time` that is earlier than that of the ad group, the campaign `end_time` will supersede the ad group `end_time`, and the request will not return an error. - In this scenario, if you call List campaigns or List ad groups, the returned campaigns or ad groups are listed with the start and end times that you assigned them, regardless of supersedence. |
| `intended_promotion_type` | `intended_promotion_type` | `string` | No | No | Specifies the intended promotion type for the campaign. Note: This field is immutable unless the campaign is in draft status. **STANDARD_AD** - For ads derived from Pins. - Supported for all objective types except for current CATALOG\_SALES and WEB\_CONVERSION, which are being deprecated. - For simplified objective types. **CATALOG** - For creating product group promotions. - Supported for CONSIDERATION and SALES objective types. - For simplified objective types. **LEGACY** - Intended only for maintaining backward compatibility. - Must be used for CATALOG\_SALES and WEB\_CONVERSION. - Supported for CATALOG\_SALES and WEB\_CONVERSION objective types. **If you do not provide this parameter, the default is:** - LEGACY for CATALOG\_SALES and WEB\_CONVERSION - STANDARD_AD for other objective types |
| `is_automated_campaign` | `is_automated_campaign` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. Specifies whether the campaign was created in the automated campaign flow. When using Automated Campaigns, a daily spend cap must be set, and Pinterest Performance+ may not be used. The following objective types are supported: - CONSIDERATION - WEB\_CONVERSION - SALES - LEADS - CATALOG\_SALES |
| `is_campaign_budget_optimization` | `is_campaign_budget_optimization` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. Determines if a campaign automatically generates ad-group level budgets given a campaign budget to maximize campaign outcome. When using campaign budgets with a lifetime spend cap, the end time must be provided. |
| `is_flexible_daily_budgets` | `is_flexible_daily_budgets` | `boolean` | No | No | Determine if a campaign has setup for flexible daily budgets, also known as "Pinterest Performance+ budgets". Flexible daily budgets are only supported for campaign budget optimization with a daily spend cap, and not a lifetime spend cap. |
| `is_ltv_optimized` | `is_ltv_optimized` | `boolean` | No | No | Specifies whether the campaign is optimized for Lifetime Value (LTV). Only available for eligible advertisers. |
| `is_performance_plus` | `is_performance_plus` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. You can only use this feature with the following objectives: - CONSIDERATION - WEB\_CONVERSION - SALES - LEADS - CATALOG\_SALES - APP\_INSTALL Enable Pinterest Performance+ for your campaign. To learn more, see [Pinterest Performance+ Setup](https://developers.pinterest.com/docs/api-features/pinterest-performance-plus-setup/) |
| `is_top_of_search` | `is_top_of_search` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. Restricted Have your ads and content appear at the top of search result lists in Pinterest. You can only use this feature with the following objectives: - CONSIDERATION - WEB\_CONVERSION - SALES - LEADS - CATALOG\_SALES You cannot use this feature with Pinterest Performance+ or Premiere Spotlight enabled. |
| `lifetime_spend_cap` | `lifetime_spend_cap` | `number` | No | No | Note: The default for this field is null, which is treated the same as 0. For campaign budget optimization, a daily or lifetime spend cap must be set, but not both. For ad group level budgets, this field must be 0 or unset. |
| `name` | `name` | `string` | Yes | No | Campaign name - 255 chars max. |
| `objective_type` | `objective_type` | `string` | Yes | No | Intended result of the campaign. You can only update objectives for draft campaigns. `WEB_SESSIONS` and `VIDEO_VIEW` objectives are deprecated. We recommend using `VIDEO_COMPLETION` as an alternative for the latter. |
| `order_line_id` | `order_line_id` | `string` | No | No | Note: To clear this field on update, set the order line ID to 0. Order line ID that appears on the invoice. The order line must not be expired. |
| `start_time` | `start_time` | `number` | No | No | Timestamp in Unix format for scheduling when ads in the campaign start to appear. Must precede any start times set for child ad groups. Defaults to current time if no time is specified. Learn about scheduling campaigns. Different start times can be set for the campaign's child ad groups, but they cannot occur before a `start_time` specified for the campaign. - If your campaign has a child ad group with a start time specified, and if you update that campaign with a `start_time` that is later than that of the ad group, the campaign `start_time` will supersede the ad group `start_time`, and the request will not return an error. - In this scenario, if you call List campaigns or List ad groups, the returned campaigns or ad groups are listed with the start and end times that you assigned them, regardless of supersedence. |
| `status` | `status` | `Inline8627` | No | No | — |
| `tracking_urls` | `tracking_urls` | `Inline8628` | No | No | — |

## Inline862

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `any_of` | `any_of` | `Array<Inline862 \| Inline863 \| Inline864 \| Inline865 \| Inline866 \| Inline867 \| Inline868 \| Inline869 \| Inline870 \| Inline871 \| Inline872 \| Inline873 \| Inline874 \| Inline875 \| Inline876 \| Inline877>` | Yes | No | — |

## Inline8620

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `age_bucket_multipliers` | `age_bucket_multipliers` | `Inline8620` | No | No | Age bucket multipliers for bid adjustments. |
| `app_type_multipliers` | `app_type_multipliers` | `Inline8621` | No | No | App type multipliers for bid adjustments. |
| `audience_multipliers` | `audience_multipliers` | `Inline8622` | No | No | Audience multipliers for bid adjustments. |
| `freq_bid_multiplier_time_window` | `freq_bid_multiplier_time_window` | `Inline8623` | No | No | The time window for frequency bid multipliers. |
| `frequency_multipliers` | `frequency_multipliers` | `Inline8624` | No | No | Frequency multipliers for bid adjustments. |
| `gender_multipliers` | `gender_multipliers` | `Inline8625` | No | No | Gender multipliers for bid adjustments. |
| `placement_multipliers` | `placement_multipliers` | `Inline8626` | No | No | Placement multipliers for bid adjustments. |

## Inline8621

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `APP_TYPE` | `APP_TYPE` | `Inline8621` | No | No | App type identifier. |

## Inline8622

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AUDIENCE_ID` | `AUDIENCE_ID` | `string` | No | No | Audience ID for the multiplier. |

## Inline8623

`object` model on the `json` wire format.

No fields are declared.

## Inline8624

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `IMPRESSION_COUNT` | `IMPRESSION_COUNT` | `string` | No | No | Impression count identifier. |

## Inline8625

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GENDER` | `GENDER` | `Inline8625` | No | No | Gender identifier. |

## Inline8626

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `PLACEMENT` | `PLACEMENT` | `Inline8626` | No | No | Placement type identifier. |

## Inline8627

`object` model on the `json` wire format.

No fields are declared.

## Inline8628

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline863

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_0` | `CUSTOM_LABEL_0` | `Inline863` | Yes | No | — |

## Inline8630

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `items` | `items` | `Array<Inline8630>` | Yes | No | — |

## Inline8631

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `APP_TYPE` | `APP_TYPE` | `Inline8631` | No | No | App type identifier. |

## Inline8632

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AUDIENCE_ID` | `AUDIENCE_ID` | `string` | No | No | Audience ID for the multiplier. |

## Inline8633

`object` model on the `json` wire format.

No fields are declared.

## Inline8634

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `IMPRESSION_COUNT` | `IMPRESSION_COUNT` | `string` | No | No | Impression count identifier. |

## Inline8635

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GENDER` | `GENDER` | `Inline8635` | No | No | Gender identifier. |

## Inline8636

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `PLACEMENT` | `PLACEMENT` | `Inline8636` | No | No | Placement type identifier. |

## Inline8637

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `boost_prospecting_ad_group_bid` | `boost_prospecting_ad_group_bid` | `boolean` | No | No | Whether to boost prospecting ad group bid. |
| `pinner_list_exclusions` | `pinner_list_exclusions` | `Array<string>` | No | No | List of campaign-level exclusion pinner list IDs. |

## Inline8638

`object` model on the `json` wire format.

No fields are declared.

## Inline8639

`object` model on the `json` wire format.

No fields are declared.

## Inline864

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_1` | `CUSTOM_LABEL_1` | `Inline864` | Yes | No | — |

## Inline8640

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline8641

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `code` | `code` | `number` | No | No | — |
| `message` | `message` | `string` | Yes | No | — |

## Inline8643

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `ad_account_id` | `ad_account_id` | `string` | No | No | Setting this field does nothing. The ad account ID gets set from the path parameter. |
| `app_id` | `app_id` | `string` | No | No | Closed beta The store ID of the mobile app for `APP_INSTALL` campaigns. |
| `app_platform` | `app_platform` | `Inline8643` | No | No | Closed beta Platform of the mobile app for `APP_INSTALL` campaigns. |
| `bid_options` | `bid_options` | `Inline8644` | No | No | Object describing an update to the campaign level bid multipliers. |
| `daily_spend_cap` | `daily_spend_cap` | `number` | No | No | Note: The default for this field is null, which is treated the same as 0. For campaign budget optimization, a daily or lifetime spend cap must be set, but not both. For ad group level budgets, this field must be 0 or unset. |
| `default_ad_group_budget_in_micro_currency` | `default_ad_group_budget_in_micro_currency` | `number` | No | No | Setting this field does nothing. |
| `end_time` | `end_time` | `number` | No | No | Note: To clear this field on update, set the end time to -1. Timestamp in Unix format for scheduling when ads in the campaign stop appearing. Must occur after any end times for child ad groups. If `end_time` is not specified for the campaign, ads run indefinitely unless you update the campaign, changing their status to `paused`. Learn about scheduling campaigns. Different end times can be set for the campaign's child ad groups, but they cannot occur after an `end_time` specified for the campaign. - If your campaign has a child ad group with an end time specified, and if you update that campaign with an `end_time` that is earlier than that of the ad group, the campaign `end_time` will supersede the ad group `end_time`, and the request will not return an error. - In this scenario, if you call List campaigns or List ad groups, the returned campaigns or ad groups are listed with the start and end times that you assigned them, regardless of supersedence. |
| `id` | `id` | `string` | Yes | No | Campaign ID, must be associated with the ad account ID provided in the path. |
| `intended_promotion_type` | `intended_promotion_type` | `string` | No | No | Specifies the intended promotion type for the campaign. Note: This field is immutable unless the campaign is in draft status. **STANDARD_AD** - For ads derived from Pins. - Supported for all objective types except for current CATALOG\_SALES and WEB\_CONVERSION, which are being deprecated. - For simplified objective types. **CATALOG** - For creating product group promotions. - Supported for CONSIDERATION and SALES objective types. - For simplified objective types. **LEGACY** - Intended only for maintaining backward compatibility. - Must be used for CATALOG\_SALES and WEB\_CONVERSION. - Supported for CATALOG\_SALES and WEB\_CONVERSION objective types. **If you do not provide this parameter, the default is:** - LEGACY for CATALOG\_SALES and WEB\_CONVERSION - STANDARD_AD for other objective types |
| `is_automated_campaign` | `is_automated_campaign` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. Specifies whether the campaign was created in the automated campaign flow. When using Automated Campaigns, a daily spend cap must be set, and Pinterest Performance+ may not be used. The following objective types are supported: - CONSIDERATION - WEB\_CONVERSION - SALES - LEADS - CATALOG\_SALES |
| `is_campaign_budget_optimization` | `is_campaign_budget_optimization` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. Determines if a campaign automatically generates ad-group level budgets given a campaign budget to maximize campaign outcome. When using campaign budgets with a lifetime spend cap, the end time must be provided. |
| `is_flexible_daily_budgets` | `is_flexible_daily_budgets` | `boolean` | No | No | Determine if a campaign has setup for flexible daily budgets, also known as "Pinterest Performance+ budgets". Flexible daily budgets are only supported for campaign budget optimization with a daily spend cap, and not a lifetime spend cap. |
| `is_ltv_optimized` | `is_ltv_optimized` | `boolean` | No | No | Specifies whether the campaign is optimized for Lifetime Value (LTV). Only available for eligible advertisers. |
| `is_performance_plus` | `is_performance_plus` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. You can only use this feature with the following objectives: - CONSIDERATION - WEB\_CONVERSION - SALES - LEADS - CATALOG\_SALES - APP\_INSTALL Enable Pinterest Performance+ for your campaign. To learn more, see [Pinterest Performance+ Setup](https://developers.pinterest.com/docs/api-features/pinterest-performance-plus-setup/) |
| `is_top_of_search` | `is_top_of_search` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. Restricted Have your ads and content appear at the top of search result lists in Pinterest. You can only use this feature with the following objectives: - CONSIDERATION - WEB\_CONVERSION - SALES - LEADS - CATALOG\_SALES You cannot use this feature with Pinterest Performance+ or Premiere Spotlight enabled. |
| `lifetime_spend_cap` | `lifetime_spend_cap` | `number` | No | No | Note: The default for this field is null, which is treated the same as 0. For campaign budget optimization, a daily or lifetime spend cap must be set, but not both. For ad group level budgets, this field must be 0 or unset. |
| `name` | `name` | `string` | No | No | Campaign name - 255 chars max. |
| `objective_type` | `objective_type` | `string` | No | No | Intended result of the campaign. You can only update objectives for draft campaigns. `WEB_SESSIONS` and `VIDEO_VIEW` objectives are deprecated. We recommend using `VIDEO_COMPLETION` as an alternative for the latter. |
| `order_line_id` | `order_line_id` | `string` | No | No | Note: To clear this field on update, set the order line ID to 0. Order line ID that appears on the invoice. The order line must not be expired. |
| `performance_plus_campaign_settings` | `performance_plus_campaign_settings` | `Inline8651` | No | No | Pinterest Performance+ campaign settings. |
| `start_time` | `start_time` | `number` | No | No | Timestamp in Unix format for scheduling when ads in the campaign start to appear. Must precede any start times set for child ad groups. Defaults to current time if no time is specified. Learn about scheduling campaigns. Different start times can be set for the campaign's child ad groups, but they cannot occur before a `start_time` specified for the campaign. - If your campaign has a child ad group with a start time specified, and if you update that campaign with a `start_time` that is later than that of the ad group, the campaign `start_time` will supersede the ad group `start_time`, and the request will not return an error. - In this scenario, if you call List campaigns or List ad groups, the returned campaigns or ad groups are listed with the start and end times that you assigned them, regardless of supersedence. |
| `status` | `status` | `string` | No | No | Entity status |
| `tracking_urls` | `tracking_urls` | `Inline8652` | No | No | — |

## Inline8644

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `age_bucket_multipliers` | `age_bucket_multipliers` | `Inline8644` | No | No | Age bucket multipliers for bid adjustments. |
| `app_type_multipliers` | `app_type_multipliers` | `Inline8645` | No | No | App type multipliers for bid adjustments. |
| `audience_multipliers` | `audience_multipliers` | `Inline8646` | No | No | Audience multipliers for bid adjustments. |
| `freq_bid_multiplier_time_window` | `freq_bid_multiplier_time_window` | `Inline8647` | No | No | The time window for frequency bid multipliers. |
| `frequency_multipliers` | `frequency_multipliers` | `Inline8648` | No | No | Frequency multipliers for bid adjustments. |
| `gender_multipliers` | `gender_multipliers` | `Inline8649` | No | No | Gender multipliers for bid adjustments. |
| `placement_multipliers` | `placement_multipliers` | `Inline8650` | No | No | Placement multipliers for bid adjustments. |
| `update_mask` | `update_mask` | `Array<string>` | No | No | List of fields to update. Only the fields in the list will be updated. |

## Inline8645

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `APP_TYPE` | `APP_TYPE` | `Inline8645` | No | No | App type identifier. |

## Inline8646

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AUDIENCE_ID` | `AUDIENCE_ID` | `string` | No | No | Audience ID for the multiplier. |

## Inline8647

`object` model on the `json` wire format.

No fields are declared.

## Inline8648

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `IMPRESSION_COUNT` | `IMPRESSION_COUNT` | `string` | No | No | Impression count identifier. |

## Inline8649

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GENDER` | `GENDER` | `Inline8649` | No | No | Gender identifier. |

## Inline865

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_2` | `CUSTOM_LABEL_2` | `Inline865` | Yes | No | — |

## Inline8650

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `PLACEMENT` | `PLACEMENT` | `Inline8650` | No | No | Placement type identifier. |

## Inline8651

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `boost_prospecting_ad_group_bid` | `boost_prospecting_ad_group_bid` | `boolean` | No | No | Whether to boost prospecting ad group bid. |
| `pinner_list_exclusions` | `pinner_list_exclusions` | `Array<string>` | No | No | List of campaign-level exclusion pinner list IDs. |

## Inline8652

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline8654

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `items` | `items` | `Array<Inline8654>` | Yes | No | — |

## Inline8655

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `APP_TYPE` | `APP_TYPE` | `Inline8655` | No | No | App type identifier. |

## Inline8656

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AUDIENCE_ID` | `AUDIENCE_ID` | `string` | No | No | Audience ID for the multiplier. |

## Inline8657

`object` model on the `json` wire format.

No fields are declared.

## Inline8658

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `IMPRESSION_COUNT` | `IMPRESSION_COUNT` | `string` | No | No | Impression count identifier. |

## Inline8659

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GENDER` | `GENDER` | `Inline8659` | No | No | Gender identifier. |

## Inline866

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_3` | `CUSTOM_LABEL_3` | `Inline866` | Yes | No | — |

## Inline8660

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `PLACEMENT` | `PLACEMENT` | `Inline8660` | No | No | Placement type identifier. |

## Inline8661

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `boost_prospecting_ad_group_bid` | `boost_prospecting_ad_group_bid` | `boolean` | No | No | Whether to boost prospecting ad group bid. |
| `pinner_list_exclusions` | `pinner_list_exclusions` | `Array<string>` | No | No | List of campaign-level exclusion pinner list IDs. |

## Inline8662

`object` model on the `json` wire format.

No fields are declared.

## Inline8663

`object` model on the `json` wire format.

No fields are declared.

## Inline8664

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline8665

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `code` | `code` | `number` | No | No | — |
| `message` | `message` | `string` | Yes | No | — |

## Inline8668

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `ad_account_id` | `ad_account_id` | `string` | No | No | Campaign's Advertiser ID. |
| `bid_options` | `bid_options` | `Inline8668` | No | No | Restricted Object describing the campaign level bid multipliers. |
| `created_time` | `created_time` | `number` | No | No | Campaign creation time. Unix timestamp in seconds. |
| `daily_spend_cap` | `daily_spend_cap` | `number` | No | No | Note: The default for this field is null, which is treated the same as 0. For campaign budget optimization, a daily or lifetime spend cap must be set, but not both. For ad group level budgets, this field must be 0 or unset. |
| `default_ad_group_budget_in_micro_currency` | `default_ad_group_budget_in_micro_currency` | `number` | No | No | When transitioning from campaign budget optimization to non-campaign budget optimization, the default_ad_group_budget_in_micro_currency will propagate to each child ad groups daily budget. Unit is micro currency of the associated advertiser account. |
| `end_time` | `end_time` | `number` | No | No | Note: To clear this field on update, set the end time to -1. Timestamp in Unix format for scheduling when ads in the campaign stop appearing. Must occur after any end times for child ad groups. If `end_time` is not specified for the campaign, ads run indefinitely unless you update the campaign, changing their status to `paused`. Learn about scheduling campaigns. Different end times can be set for the campaign's child ad groups, but they cannot occur after an `end_time` specified for the campaign. - If your campaign has a child ad group with an end time specified, and if you update that campaign with an `end_time` that is earlier than that of the ad group, the campaign `end_time` will supersede the ad group `end_time`, and the request will not return an error. - In this scenario, if you call List campaigns or List ad groups, the returned campaigns or ad groups are listed with the start and end times that you assigned them, regardless of supersedence. |
| `id` | `id` | `string` | Yes | No | Campaign ID, must be associated with the ad account ID provided in the path. |
| `intended_promotion_type` | `intended_promotion_type` | `string` | No | No | Specifies the intended promotion type for the campaign. Note: This field is immutable unless the campaign is in draft status. **STANDARD_AD** - For ads derived from Pins. - Supported for all objective types except for current CATALOG\_SALES and WEB\_CONVERSION, which are being deprecated. - For simplified objective types. **CATALOG** - For creating product group promotions. - Supported for CONSIDERATION and SALES objective types. - For simplified objective types. **LEGACY** - Intended only for maintaining backward compatibility. - Must be used for CATALOG\_SALES and WEB\_CONVERSION. - Supported for CATALOG\_SALES and WEB\_CONVERSION objective types. **If you do not provide this parameter, the default is:** - LEGACY for CATALOG\_SALES and WEB\_CONVERSION - STANDARD_AD for other objective types |
| `is_automated_campaign` | `is_automated_campaign` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. Specifies whether the campaign was created in the automated campaign flow. When using Automated Campaigns, a daily spend cap must be set, and Pinterest Performance+ may not be used. The following objective types are supported: - CONSIDERATION - WEB\_CONVERSION - SALES - LEADS - CATALOG\_SALES |
| `is_campaign_budget_optimization` | `is_campaign_budget_optimization` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. Determines if a campaign automatically generates ad-group level budgets given a campaign budget to maximize campaign outcome. When using campaign budgets with a lifetime spend cap, the end time must be provided. |
| `is_carting` | `is_carting` | `boolean` | No | No | Whether the campaign contains a carting(where-to-buy link) ad. |
| `is_flexible_daily_budgets` | `is_flexible_daily_budgets` | `boolean` | No | No | Determine if a campaign has setup for flexible daily budgets, also known as "Pinterest Performance+ budgets". Flexible daily budgets are only supported for campaign budget optimization with a daily spend cap, and not a lifetime spend cap. |
| `is_ltv_optimized` | `is_ltv_optimized` | `boolean` | No | No | Specifies whether the campaign is optimized for Lifetime Value (LTV). Only available for eligible advertisers. |
| `is_performance_plus` | `is_performance_plus` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. You can only use this feature with the following objectives: - CONSIDERATION - WEB\_CONVERSION - SALES - LEADS - CATALOG\_SALES - APP\_INSTALL Enable Pinterest Performance+ for your campaign. To learn more, see [Pinterest Performance+ Setup](https://developers.pinterest.com/docs/api-features/pinterest-performance-plus-setup/) |
| `is_top_of_search` | `is_top_of_search` | `boolean` | No | No | Note: This field is immutable unless the campaign is in draft status. Restricted Have your ads and content appear at the top of search result lists in Pinterest. You can only use this feature with the following objectives: - CONSIDERATION - WEB\_CONVERSION - SALES - LEADS - CATALOG\_SALES You cannot use this feature with Pinterest Performance+ or Premiere Spotlight enabled. |
| `lifetime_spend_cap` | `lifetime_spend_cap` | `number` | No | No | Note: The default for this field is null, which is treated the same as 0. For campaign budget optimization, a daily or lifetime spend cap must be set, but not both. For ad group level budgets, this field must be 0 or unset. |
| `name` | `name` | `string` | No | No | Campaign name - 255 chars max. |
| `objective_type` | `objective_type` | `string` | Yes | No | Campaign objective type. If set as one of ["AWARENESS", "CONSIDERATION", "WEB_CONVERSION", "CATALOG_SALES", "VIDEO_COMPLETION", "CTV_CONSIDERATION"] the campaign is considered as a Campaign Budget Optimization (CBO) campaign, meaning budget needs to be set at the campaign level rather than at the ad group level. ["WEB_SESSIONS"] is DEPRECATED. `VIDEO_VIEW` is deprecated; use `VIDEO_COMPLETION` instead. `CTV_CONSIDERATION` is in BETA. For update, only draft campaigns may update objective type. |
| `order_line_id` | `order_line_id` | `string` | No | No | Note: To clear this field on update, set the order line ID to 0. Order line ID that appears on the invoice. The order line must not be expired. |
| `performance_plus_campaign_settings` | `performance_plus_campaign_settings` | `Inline8675` | No | No | Pinterest Performance+ campaign settings. |
| `start_time` | `start_time` | `number` | No | No | Timestamp in Unix format for scheduling when ads in the campaign start to appear. Must precede any start times set for child ad groups. Defaults to current time if no time is specified. Learn about scheduling campaigns. Different start times can be set for the campaign's child ad groups, but they cannot occur before a `start_time` specified for the campaign. - If your campaign has a child ad group with a start time specified, and if you update that campaign with a `start_time` that is later than that of the ad group, the campaign `start_time` will supersede the ad group `start_time`, and the request will not return an error. - In this scenario, if you call List campaigns or List ad groups, the returned campaigns or ad groups are listed with the start and end times that you assigned them, regardless of supersedence. |
| `status` | `status` | `string` | No | No | Entity status |
| `summary_status` | `summary_status` | `Inline8676` | No | No | — |
| `tracking_urls` | `tracking_urls` | `Inline8677` | No | No | — |
| `type` | `type` | `string` | No | No | Always "campaign". |
| `updated_time` | `updated_time` | `number` | No | No | UTC timestamp. Last update time. |

## Inline8669

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `APP_TYPE` | `APP_TYPE` | `Inline8669` | No | No | App type identifier. |

## Inline867

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CUSTOM_LABEL_4` | `CUSTOM_LABEL_4` | `Inline867` | Yes | No | — |

## Inline8670

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AUDIENCE_ID` | `AUDIENCE_ID` | `string` | No | No | Audience ID for the multiplier. |

## Inline8671

`object` model on the `json` wire format.

No fields are declared.

## Inline8672

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `IMPRESSION_COUNT` | `IMPRESSION_COUNT` | `string` | No | No | Impression count identifier. |

## Inline8673

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GENDER` | `GENDER` | `Inline8673` | No | No | Gender identifier. |

## Inline8674

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `PLACEMENT` | `PLACEMENT` | `Inline8674` | No | No | Placement type identifier. |

## Inline8675

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `boost_prospecting_ad_group_bid` | `boost_prospecting_ad_group_bid` | `boolean` | No | No | Whether to boost prospecting ad group bid. |
| `pinner_list_exclusions` | `pinner_list_exclusions` | `Array<string>` | No | No | List of campaign-level exclusion pinner list IDs. |

## Inline8676

`object` model on the `json` wire format.

No fields are declared.

## Inline8677

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `audience_verification` | `audience_verification` | `Array<string>` | No | No | — |
| `buyable_button` | `buyable_button` | `Array<string>` | No | No | — |
| `click` | `click` | `Array<string>` | No | No | — |
| `engagement` | `engagement` | `Array<string>` | No | No | — |
| `impression` | `impression` | `Array<string>` | No | No | — |

## Inline868

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GOOGLE_PRODUCT_CATEGORY_6` | `GOOGLE_PRODUCT_CATEGORY_6` | `Inline868` | Yes | No | — |

## Inline8680

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `CAMPAIGN_ID` | `CAMPAIGN_ID` | `string` | No | No | The ID of the campaing that this metrics belongs to. Returned as long as aggregate_report_rows is not true. |
| `DATE` | `DATE` | `string` | No | No | Current metrics date. Only returned when granularity is a time-based value (`DAY`, `HOUR`, `WEEK`, `MONTH`) |

## Inline8682

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `ad_groups` | `ad_groups` | `Array<Inline8682>` | Yes | No | — |
| `budget_duration_type` | `budget_duration_type` | `Inline8688` | Yes | No | Duration type of the budget |
| `daily_spend_cap` | `daily_spend_cap` | `number` | No | No | Campaign daily spend cap. When the budget_duration_type is FIXED_DAILY or FLEXIBLE_DAILY, this field is required. |
| `end_date` | `end_date` | `string` | No | No | End date of the date range for an ad campaign, pattern YYYY-MM-DD. Required when budget_duration_type is LIFETIME. |
| `lifetime_spend_cap` | `lifetime_spend_cap` | `number` | No | No | Campaign total spending cap. When the budget_duration_type is LIFETIME, this field is required. The end date field is also required for lifetime spending. |
| `objective_type` | `objective_type` | `string` | Yes | No | Objective type for delivery estimates. |
| `start_date` | `start_date` | `string` | Yes | No | Start date of the date range for an ad campaign, pattern YYYY-MM-DD. |

## Inline8683

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `conversion_tag_v3_goal_metadata` | `conversion_tag_v3_goal_metadata` | `Inline8683` | No | No | — |
| `frequency_goal_metadata` | `frequency_goal_metadata` | `Inline8684` | No | No | Frequency target can only be between 2 and 20 |
| `scrollup_goal_metadata` | `scrollup_goal_metadata` | `Inline8685` | No | No | — |

## Inline8684

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `frequency` | `frequency` | `number` | No | No | — |
| `timerange` | `timerange` | `string` | No | No | — |

## Inline8685

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `scrollup_goal_value_in_micro_currency` | `scrollup_goal_value_in_micro_currency` | `string` | No | No | — |

## Inline8686

`object` model on the `json` wire format.

No fields are declared.

## Inline8687

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `AGE_BUCKET` | `AGE_BUCKET` | `Array<string>` | No | No | **Legacy field.** Predefined age ranges. We recommend using MINIMUM_AGE and MAXIMUM_AGE instead for more flexible targeting. Cannot be combined with MINIMUM_AGE/MAXIMUM_AGE. If neither AGE_BUCKET nor MINIMUM_AGE/MAXIMUM_AGE are specified, all ages will be targeted. |
| `APPTYPE` | `APPTYPE` | `Array<string>` | No | No | Allowed devices. If the APPTYPE field is missing, the default behavior in terms of ad delivery is that **All devices/apptypes** will be targeted. |
| `AUDIENCE_EXCLUDE` | `AUDIENCE_EXCLUDE` | `Array<unknown>` | No | No | Excluded customer list IDs. Used to drive new customer acquisition goals. For example: ["2542620905475"]. Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_EXCLUDE field is missing, the default behavior in terms of ad delivery is that **No users will be excluded**. |
| `AUDIENCE_INCLUDE` | `AUDIENCE_INCLUDE` | `Array<unknown>` | No | No | Targeted customer list IDs. For example: ["2542620905473"]. Audience lists need to have at least 100 people with Pinterest accounts in them Audience lists need to have at least 100 people with Pinterest accounts in them. If the AUDIENCE_INCLUDE field is missing, the default behavior in terms of ad delivery is that **All users will be included**. |
| `GENDER` | `GENDER` | `Array<string>` | No | No | Targeted genders. Values: ["unknown","male","female"]. If the GENDER field is missing, the default behavior in terms of ad delivery is that **All genders will be targeted**. |
| `GEO` | `GEO` | `Array<string>` | No | No | Region codes or postal codes to include for targeting. Region codes represent broader geographical areas. Example: `US-CA` is the region code for California in the United States. Postal codes represent more granular, specific areas. Example: `94103` is a postal code for a specifc area in San Francisco, California, U.S.A. For each ad group, use only one of these methods, depending on which fits your targeting needs. Do not use both. For example, either specify a broader region code like `US-CA` or a more granular postal code within that regon, such as `94103`. You can specify multiple region codes or postal codes in an array, depending on which method you choose. Precede a region code array with the `region_codes` key and a postal code value with the `postal_codes` key. Examples: ``` "geo": &#123; "region_codes": ["US-CA"] &#125; ``` ``` "geo": &#123; "postal_codes": ["94103"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION`. If you do not specifiy a `GEO` code, only `LOCATION` values will be targeted (See `LOCATION` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `GEO_EXCLUDE` | `GEO_EXCLUDE` | `Array<string>` | No | No | Closed beta Region codes or postal codes to exclude from the targeting inclusion area. See `GEO` parameter in this targeting spec for rules, syntax, and other information. |
| `INTEREST` | `INTEREST` | `Array<string>` | No | No | Array of interest object IDs. If the INTEREST field is missing, the default behavior in terms of ad delivery is that **All interests will be targeted**. |
| `LOCALE` | `LOCALE` | `Array<string>` | No | No | 24 ISO 639-1 two-letter language codes. If the LOCALE field is not included in the request, all languages are targeted. |
| `LOCATION` | `LOCATION` | `Array<string>` | No | No | Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to include for targeting. Precede country code values with the `country_codes` key and metro code values with `metro_codes` key. Example: ``` "location": &#123; "country_codes": ["US", "CA"], "metro_codes": ["501", "602"] &#125; ``` For each ad group, specify at least one `GEO` or `LOCATION` code. If you do not specify a `LOCATION` code, only `GEO` values will be targeted (See `GEO` parameter in this targeting spec.). Learn how to get a current, complete list of codes. |
| `LOCATION_EXCLUDE` | `LOCATION_EXCLUDE` | `Array<string>` | No | No | Closed beta Metropolitan codes and/or ISO-Alpha-2, two-letter country codes to exclude from targeting. See `LOCATION` parameter in this targeting spec for rules, syntax, and other information. |
| `MAXIMUM_AGE` | `MAXIMUM_AGE` | `string` | No | No | Maximum age to target (inclusive). Values: "18", "19", ..., "65", "65+". Must be used together with `MINIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `MINIMUM_AGE` | `MINIMUM_AGE` | `string` | No | No | Minimum age to target (inclusive). Values: "18", "19", ..., "65". Note: 65+ is not allowed for minimum age. Must be used together with `MAXIMUM_AGE`. Cannot be combined with `AGE_BUCKET`. If neither `MINIMUM_AGE`/`MAXIMUM_AGE` nor `AGE_BUCKET` are specified, all ages will be targeted. |
| `SHOPPING_RETARGETING` | `SHOPPING_RETARGETING` | `Array<Inline8687>` | No | No | Array of object: lookback_window [Integer]: Number of days ago to start lookback timeframe for dynamic retargeting tag_types [Array of integer]: Event types to target for dynamic retargeting exclusion_window [Integer]: Number of days ago to stop lookback timeframe for dynamic retargeting |
| `TARGETING_STRATEGY` | `TARGETING_STRATEGY` | `Array<string>` | No | No | — |

## Inline8688

`object` model on the `json` wire format.

No fields are declared.

## Inline869

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GOOGLE_PRODUCT_CATEGORY_5` | `GOOGLE_PRODUCT_CATEGORY_5` | `Inline869` | Yes | No | — |

## Inline8690

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `curves` | `curves` | `Array<Inline8690>` | No | No | Estimated curves. Each curve will pertain to a single estimation type. |
| `derived_metrics` | `derived_metrics` | `Inline8692` | No | No | Derived metrics for an ad campaign. |
| `max_potential_spend` | `max_potential_spend` | `number` | No | No | Maximum potential spend estimate. |

## Inline8691

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `budget` | `budget` | `number` | Yes | No | The budget value of the point. |
| `double_y` | `double_y` | `number` | Yes | No | Y value as a decimal. |
| `max_y` | `max_y` | `number` | Yes | No | The maximum Y value of the point. |
| `min_y` | `min_y` | `number` | Yes | No | The minimum Y value of the point. |
| `y` | `y` | `number` | Yes | No | The expected Y value of the point. |

## Inline8692

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `cpc` | `cpc` | `number` | No | No | Estimated cost per click. |
| `cpc_lower` | `cpc_lower` | `number` | No | No | Lower estimate of the cost per click. |
| `cpc_upper` | `cpc_upper` | `number` | No | No | Upper estimate of the cost per click. |
| `cpm` | `cpm` | `number` | No | No | Estimated cost per thousand impressions. |
| `cpm_lower` | `cpm_lower` | `number` | No | No | Lower estimate of cost per thousand impressions. |
| `cpm_upper` | `cpm_upper` | `number` | No | No | Upper estimate of cost per thousand impressions. |
| `lifetime_frequency` | `lifetime_frequency` | `number` | No | No | Estimated lifetime frequency. |
| `lifetime_frequency_lower` | `lifetime_frequency_lower` | `number` | No | No | Lower estimate of lifetime frequency. |
| `lifetime_frequency_upper` | `lifetime_frequency_upper` | `number` | No | No | Upper estimate of lifetime frequency. |
| `lifetime_impression` | `lifetime_impression` | `number` | No | No | Estimated lifetime impressions. |
| `lifetime_impression_lower` | `lifetime_impression_lower` | `number` | No | No | Lower estimate of lifetime impressions. |
| `lifetime_impression_upper` | `lifetime_impression_upper` | `number` | No | No | Upper estimate of lifetime impressions. |
| `lifetime_reach` | `lifetime_reach` | `number` | No | No | Estimated lifetime reach. |
| `lifetime_reach_lower` | `lifetime_reach_lower` | `number` | No | No | Lower estimate of lifetime reach. |
| `lifetime_reach_upper` | `lifetime_reach_upper` | `number` | No | No | Upper estimate of lifetime reach. |
| `weekly_click` | `weekly_click` | `number` | No | No | Estimated weekly clicks. |
| `weekly_click_lower` | `weekly_click_lower` | `number` | No | No | Lower estimate of weekly clicks. |
| `weekly_click_upper` | `weekly_click_upper` | `number` | No | No | Upper estimate of weekly clicks. |
| `weekly_frequency` | `weekly_frequency` | `number` | No | No | Estimated weekly frequency. |
| `weekly_frequency_lower` | `weekly_frequency_lower` | `number` | No | No | Lower estimate of weekly frequency. |
| `weekly_frequency_upper` | `weekly_frequency_upper` | `number` | No | No | Upper estimate of weekly frequency. |
| `weekly_impression` | `weekly_impression` | `number` | No | No | Estimated weekly impressions. |
| `weekly_impression_lower` | `weekly_impression_lower` | `number` | No | No | Lower estimate of weekly impressions. |
| `weekly_impression_upper` | `weekly_impression_upper` | `number` | No | No | Upper estimate of weekly impressions. |
| `weekly_reach` | `weekly_reach` | `number` | No | No | Estimated weekly reach. |
| `weekly_reach_lower` | `weekly_reach_lower` | `number` | No | No | Lower estimate of weekly reach. |
| `weekly_reach_upper` | `weekly_reach_upper` | `number` | No | No | Upper estimate of weekly reach. |

## Inline8695

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `data` | `data` | `Array<Inline8695>` | No | No | — |

## Inline8698

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `bookmark` | `bookmark` | `string` | No | No | — |
| `items` | `items` | `Array<Inline8698>` | Yes | No | — |

## Inline87

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `data` | `data` | `Inline87` | No | No | — |
| `exceptions` | `exceptions` | `unknown` | No | No | — |

## Inline870

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GOOGLE_PRODUCT_CATEGORY_4` | `GOOGLE_PRODUCT_CATEGORY_4` | `Inline870` | Yes | No | — |

## Inline8700

`object` model on the `json` wire format.

No fields are declared.

## Inline8702

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `created_time` | `created_time` | `string` | Yes | No | Timestamp when the conversion deletion request was succesfully created. |
| `processed_time` | `processed_time` | `string` | No | No | Timestamp when the conversion deletion request was processed. |
| `request_id` | `request_id` | `string` | Yes | No | Unique identifier of the conversion deletion request |
| `status` | `status` | `Inline8702` | Yes | No | Represents the status of the conversion deletion request. "PENDING" or "SUBMITTED". "PENDING" meaning the Advertiser can cancel the request. The advertiser has a 7 day time window after the conversion deletion request was submitted to cancel it. "SUBMITTED" meaning that the Data Deletion process has begun and can no longer be canceled. |

## Inline8704

`object` model on the `json` wire format.

No fields are declared.

## Inline8706

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `created_time` | `created_time` | `string` | Yes | No | Timestamp when the conversion deletion request was succesfully created. |
| `processed_time` | `processed_time` | `string` | No | No | Timestamp when the conversion deletion request was processed. |
| `request_id` | `request_id` | `string` | Yes | No | Unique identifier of the conversion deletion request |
| `status` | `status` | `Inline8706` | Yes | No | Represents the status of the conversion deletion request. "PENDING" or "SUBMITTED". "PENDING" meaning the Advertiser can cancel the request. The advertiser has a 7 day time window after the conversion deletion request was submitted to cancel it. "SUBMITTED" meaning that the Data Deletion process has begun and can no longer be canceled. |

## Inline8709

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `created_time` | `created_time` | `string` | Yes | No | Timestamp when the conversion deletion request was succesfully created. |
| `processed_time` | `processed_time` | `string` | No | No | Timestamp when the conversion deletion request was processed. |
| `request_id` | `request_id` | `string` | Yes | No | Unique identifier of the conversion deletion request |
| `status` | `status` | `Inline8709` | Yes | No | Represents the status of the conversion deletion request. "PENDING" or "SUBMITTED". "PENDING" meaning the Advertiser can cancel the request. The advertiser has a 7 day time window after the conversion deletion request was submitted to cancel it. "SUBMITTED" meaning that the Data Deletion process has begun and can no longer be canceled. |

## Inline871

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GOOGLE_PRODUCT_CATEGORY_3` | `GOOGLE_PRODUCT_CATEGORY_3` | `Inline871` | Yes | No | — |

## Inline8712

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `ingestion_source` | `ingestion_source` | `string` | Yes | No | List of ingestion sources for a conversion event. |
| `lookback_period` | `lookback_period` | `string` | Yes | No | Days in lookback window in the GET Conversion EQS response. |
| `overall_status` | `overall_status` | `string` | Yes | No | Overall status of event quality score. |
| `quality_components` | `quality_components` | `Inline8712` | Yes | No | Set of quality components, with each component containing a event coverage and details. |
| `source_platform` | `source_platform` | `string` | Yes | No | List of source platforms for a conversion event. |

## Inline8713

`object` model on the `json` wire format.

No fields are declared.

## Inline8714

`object` model on the `json` wire format.

No fields are declared.

## Inline8715

`object` model on the `json` wire format.

No fields are declared.

## Inline8716

`object` model on the `json` wire format.

No fields are declared.

## Inline8717

`object` model on the `json` wire format.

No fields are declared.

## Inline8718

`object` model on the `json` wire format.

No fields are declared.

## Inline8719

`object` model on the `json` wire format.

No fields are declared.

## Inline872

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GOOGLE_PRODUCT_CATEGORY_2` | `GOOGLE_PRODUCT_CATEGORY_2` | `Inline872` | Yes | No | — |

## Inline8720

`object` model on the `json` wire format.

No fields are declared.

## Inline8721

`object` model on the `json` wire format.

No fields are declared.

## Inline8722

`object` model on the `json` wire format.

No fields are declared.

## Inline8725

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `ad_account_id` | `ad_account_id` | `string` | Yes | No | Ad account ID. |
| `status` | `status` | `Inline8725` | No | No | — |
| `code_snippet` | `code_snippet` | `string` | No | No | Tag code snippet. |
| `configs` | `configs` | `Inline8726` | No | No | — |
| `enhanced_match_status` | `enhanced_match_status` | `Inline8727` | No | No | The enhanced match status of the tag |
| `id` | `id` | `string` | No | No | Tag ID. |
| `last_fired_time_ms` | `last_fired_time_ms` | `number` | No | No | Time for the last event fired. |
| `name` | `name` | `string` | No | No | Conversion tag name. |
| `version` | `version` | `string` | No | No | Version number. |

## Inline8726

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `aem_db_enabled` | `aem_db_enabled` | `boolean` | No | No | Whether Automatic Enhanced Match birthdate is enabled. See [Enhanced match](https://help.pinterest.com/en/business/article/enhanced-match) for more information. |
| `aem_enabled` | `aem_enabled` | `boolean` | No | No | Whether Automatic Enhanced Match email is enabled. See [Enhanced match](https://help.pinterest.com/en/business/article/enhanced-match) for more information. |
| `aem_external_id_enabled` | `aem_external_id_enabled` | `boolean` | No | No | Whether Automatic Enhanced Match location is enabled. See [Enhanced match](https://help.pinterest.com/en/business/article/enhanced-match) for more information. |
| `aem_fnln_enabled` | `aem_fnln_enabled` | `boolean` | No | No | Whether Automatic Enhanced Match name is enabled. See [Enhanced match](https://help.pinterest.com/en/business/article/enhanced-match) for more information. |
| `aem_ge_enabled` | `aem_ge_enabled` | `boolean` | No | No | Whether Automatic Enhanced Match gender is enabled. See [Enhanced match](https://help.pinterest.com/en/business/article/enhanced-match) for more information. |
| `aem_loc_enabled` | `aem_loc_enabled` | `boolean` | No | No | Whether Automatic Enhanced Match location is enabled. See [Enhanced match](https://help.pinterest.com/en/business/article/enhanced-match) for more information. |
| `aem_ph_enabled` | `aem_ph_enabled` | `boolean` | No | No | Whether Automatic Enhanced Match phone is enabled. See [Enhanced match](https://help.pinterest.com/en/business/article/enhanced-match) for more information. |
| `md_frequency` | `md_frequency` | `number` | No | No | Metadata ingestion frequency. |
| `no_code_capi_domains` | `no_code_capi_domains` | `Array<string>` | No | No | List of advertiser subdomains configured for NoCodeCAPI calls. |

## Inline8727

`object` model on the `json` wire format.

No fields are declared.

## Inline873

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `GOOGLE_PRODUCT_CATEGORY_1` | `GOOGLE_PRODUCT_CATEGORY_1` | `Inline873` | Yes | No | — |

## Inline8730

`object` model on the `json` wire format.

| Field | Wire name | Type | Required | Nullable | Description |
| --- | --- | --- | --- | --- | --- |
| `items` | `items` | `Array<Inline8730>` | Yes | No | — |
