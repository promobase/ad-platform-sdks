---
"@openpromo/sdk-runtime": minor
---

Add `createOAuthFlow()` and `OAuthFlow` to encapsulate authorization and
callback completion while preserving each provider adapter's native typed
discovery methods.
