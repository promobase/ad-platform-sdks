---
"@openpromo/ad-platforms-cli": minor
---

Add a Commander and Zod based CLI package with a Meta command tree for Instagram, Facebook, and Threads publishing operations, plus a stdio MCP server that exposes the same operations as tools.
